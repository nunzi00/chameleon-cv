## Ejemplos

```bash
cv llm key list
# Fichero de claves: /home/ada/.config/chameleon-cv/keys.json
# openai: variable de entorno
# anthropic: ninguna
```

- Dice de dónde sale cada clave (variable de entorno, fichero de claves o ninguna) y avisa si el fichero tiene permisos abiertos o no es válido. Nunca muestra valores.
