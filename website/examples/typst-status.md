## Ejemplos

```bash
cv typst status             # qué binario se usaría (--typst-path, CHAMELEON_TYPST, caché, PATH), su versión y si es utilizable
```

- Código 0 si hay un Typst utilizable; en otro caso, código 2 y la instrucción para instalarlo.
