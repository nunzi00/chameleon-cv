## Ejemplos

```bash
cv llm key remove openai     # elimina la clave del fichero de claves; si no estaba, lo dice
```

- Solo afecta al fichero de claves: una clave definida en `CHAMELEON_OPENAI_API_KEY` sigue en el entorno.
