## Ejemplos

```bash
cv llm cache clear       # vacía la caché local de respuestas del co-piloto (ficheros 0600 en la caché de usuario)
```

- La caché hace que repetir una orden sea gratis e idéntico; `--no-cache` en cada orden la salta sin vaciarla.
