## Ejemplos

```bash
cv theme path classic            # ruta absoluta del directorio del tema, para mirarlo o copiar sus ficheros
$EDITOR "$(cv theme path mio)/theme.toml"
```

- Si el tema existe pero no es utilizable, imprime igualmente la ruta con un aviso: sirve para arreglarlo.
