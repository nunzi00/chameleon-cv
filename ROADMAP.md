# Roadmap del Proyecto: Chameleon CV

> **Estado a 1-sep-2026: no queda ninguna tarea abierta.** Los nueve hitos están cerrados y publicados (última versión, v1.16.0). Las dos últimas que quedaban —T-6.4 (npm y Homebrew) y T-6.5 (firma y notarización)— las **descartó el PO el 1-sep**: ninguna es trabajo que el proyecto pueda cerrar por su cuenta (exigen cuentas y certificados de una persona) y ninguna cambia lo que el producto hace. Lo que entre a partir de aquí nacerá de algo medido o de un encargo, como todo lo demás.

> **Estado (2026-08-28): Hito 1 (MVP) completado — victoria declarada por el Director de Ingeniería.** Flujo `cv validate → cv build-profile → cv generate-cv` operativo, 292 tests, 100 % de cobertura en toda la lógica. **Hito 2 (Adaptación Dinámica) declarado COMPLETO Y VALIDADO por el Director de Ingeniería el 2026-08-28.** **Hito 2.5 (Consolidación y Calidad de Vida) declarado COMPLETO Y VALIDADO por el Director de Ingeniería el 2026-08-28** (392 tests, 100 % de cobertura). **Hito 3 (Representación Profesional): T-3.1–T-3.4 cerradas el 2026-08-28 (`docs/typst-integration.md` aprobado; motor Typst contenido, `cv typst install|status`, diseño de referencia y documentación); pendiente de declaración por el Director de Ingeniería.**

## Stack Tecnológico: Node.js con TypeScript

## Épica: Generador de CVs Dinámicos

### Hito 1: MVP - Generador por Especialidad (Target: 3 días)

Replanificado el 2026-08-28 por el Director de Ingeniería: la lógica de selección por especialidad (T-1.5) se especifica e implementa antes que sus consumidores. Orden de ejecución: **T-1.5 → T-1.7 → T-1.4 → T-1.8**. **Hito 1 completado el 2026-08-28** (MVP: flujo `cv validate → cv build-profile → cv generate-cv`).

-   [x] **T-1.1: [CORE] Diseño del Esquema de Datos Unificado.** Definir una interfaz TypeScript ('MasterProfile') que represente de forma agnóstica toda la información de un candidato (datos personales, experiencia, proyectos, skills, logros).
    -   Hecho 2026-08-28: `src/core/schema/` (esquema zod como fuente única, tipos derivados, validación y saneado en tiempo de ejecución, unicidad de ids; 100 % de cobertura). Commits 9aee96e y a700395.
-   [x] **T-1.2: [PARSER] Implementar parser para Markdown.** Crear un módulo usando una librería robusta como `marked` o `unified` para leer los ficheros .md y mapearlos a la interfaz 'MasterProfile'.
    -   Hecho 2026-08-28: formato aprobado (`docs/formato-dataset.md`, disposición A) e implementado en `src/parsers/markdown/` (unified/remark + yaml failsafe, errores `fichero:línea`) y `src/parsers/dataset/` (recorrido estricto, enlaces acotados, límites, fusión con procedencia, validación global). Dataset de ejemplo en `tests/fixtures/dataset/`. Node ≥ 22.12 (`engines`).
-   [x] **T-1.3: [PARSER] Implementar parser para CSV.** Usar `csv-parse` para leer skills o proyectos desde un CSV y añadirlos al 'MasterProfile'.
    -   Hecho 2026-08-28: formato aprobado (`docs/formato-csv.md`; certificaciones en CSV) e implementado en `src/parsers/csv/` (`csv-parse` 7, cabecera con claves del esquema, «;» detectado, multivalor «|», ids posicionales, errores fichero:línea). `skills.csv` y `certifications.csv` en el dataset de ejemplo.
-   [x] **T-1.4: [CLI] Comando `build-profile`.** Compilador de fuentes: recorre `data/sources/`, valida y escribe el artefacto canónico `data/dist/profile.json` (`docs/arquitectura.md` §2.3–2.4); incluye `validate`. Se ejecuta tras T-1.7.
    -   Hecho 2026-08-28: binario **`cv`** (`bin` en package.json; `npm run cv -- …` con ts-node). `cv build-profile [--data] [--out] [-v]` (silencioso en éxito; errores `fichero:línea` en stderr; artefacto atómico con permisos 0600 en `src/artifact/`) y `cv validate`. Códigos de salida: 0 ok, 1 datos inválidos, 2 uso o entorno. CLI testeable con contexto inyectado (`src/cli/`).
-   [x] **T-1.5: [CORE] `SelectorEngine`.** Módulo de lógica pura que filtra un `MasterProfile` según la especialidad (tags): algoritmo de selección, firma principal e informe explicable. Especificación aprobada en `docs/selector-engine.md`.
    -   Hecho 2026-08-28: `src/core/selection/` (`selectForSpecialty`, `specialtyVocabulary`, `relevanceOf`; informe con motivos universal/matched/via-achievements/no-match). Suite con los ejemplos de la especificación y los seis invariantes canónicos (invariante 5 precisado en §2.5).
-   [x] **T-1.6: [TESTS] Pruebas unitarias.** Configurar `Jest` o `Vitest` para testear la lógica de los parsers y las funciones de selección de datos. Cobertura del 100% en la lógica de negocio.
    -   En curso 2026-08-28: harness adelantado (Vitest 4 + cobertura v8 con umbral 100 % sobre `src/core/**` y `src/parsers/**`; commit a700395). Umbral aplicado a `src/core`, `src/parsers`, `src/renderers`, `src/artifact`, `src/cli` y `src/shared`; solo queda fuera `src/index.ts` (cableado del proceso). Cerrada 2026-08-28 con T-1.8: 292 tests y el flujo completo documentado en README.md.
-   [x] **T-1.7: [GENERATOR] `MarkdownRenderer`.** Convierte un `MasterProfile` (seleccionado) en un CV en Markdown mediante Handlebars y un modelo de vista; plantilla base aprobada en `docs/selector-engine.md` §5.
    -   Hecho 2026-08-28: `src/renderers/markdown/` (`buildCvView` puro, etiquetas es/en por locale, fechas con Intl, orden cronológico, Handlebars `noEscape` + normalización) y `templates/cv.md.hbs`; golden `tests/fixtures/golden/cv-backend.md` = §5.4.
-   [x] **T-1.8: [CLI] Comando `generate-cv`.** Orquesta `profile.json` → `SelectorEngine` → `MarkdownRenderer` → `output/<cv>.md`. Culminación del MVP.
    -   Hecho 2026-08-28: `cv generate-cv [-s <id>] [-p artefacto] [-o salida] [-t plantilla] [-l locale] [--explain] [--stdout]`; lee y re-valida `profile.json`, avisa si alguna fuente es más reciente (nunca reconstruye por su cuenta), selecciona por especialidad o genera el CV completo, escribe `output/cv-<nombre>[-<especialidad>].md` con permisos 0600. README de uso con el flujo `validate → build-profile → generate-cv`.

### Hito 2: Evolución - Adaptación por Oferta de Empleo (Target: 1 semana post-MVP)

-   [x] **T-2.1: [NLP] Módulo de extracción de keywords de ofertas.** Implementar una función que reciba el texto de una oferta y extraiga entidades clave (tecnologías, skills) usando librerías como `natural` o, inicialmente, una combinación de regex y diccionarios predefinidos para mantenerlo ligero.
    -   Hecho 2026-08-28 (`docs/scoring.md` aprobado): `src/core/keywords/` — contrato `JobRequirements` (zod), el perfil como diccionario (tags + nombres y alias de skills), énfasis por secciones es/en, años exigidos, matching con límites propios y enmascarado, carencias con diccionario incorporado. Sin `natural`.
-   [x] **T-2.2: [CORE] Lógica de 'scoring' y selección.** Diseñar un algoritmo que puntúe los logros y skills del 'MasterProfile' en función de las keywords extraídas de la oferta.
    -   Hecho 2026-08-28: `src/core/scoring/` — especialidad virtual `offer` sobre `selectForSpecialty` sin cambios (`tailorToOffer`, con `--specialty` real opcional), `scoreSelection` aditivo (contenedor = propio + logros), reordenación solo de logros y skills, `MatchReport` con cobertura; informe `--explain` de adecuación (`formatMatchReport`).
-   [x] **T-2.3: [GENERATOR] Mejorar el motor de plantillas.** Permitir que la plantilla renderice dinámicamente solo los 'N' mejores puntos para cada sección, basado en el scoring.
    -   Hecho 2026-08-28 (`docs/trimming-cli.md` aprobado): `src/core/scoring/trim.ts` (`applyLimits`, `keepTop`, `COMPACT_LIMITS`; ranking único, universales puntúan 0, cinco invariantes en la suite) y `summary.ts` (`summarizeMatch`). El recorte vive en el núcleo, no en la plantilla.
-   [x] **T-2.4: [CLI] Ampliar la CLI.** Añadir el comando `generate --from-job-offer <path_to_offer.txt>`.
    -   Hecho 2026-08-28: `cv generate-cv --from-job-offer <fichero|->` con `--top-n`, `--max-skills`, `--max-projects`, `--max-certifications`, `--compact` y `--explain` ampliado (adecuación + recortes); sufijo de oferta en la salida; `cv analyze-offer <oferta>` (resumen legible, `--explain`, `--json`, stdin con `-`). README actualizado.
-   [x] **T-2.5: [PARSER] Soporte básico para PDF (entrada).** Investigar e implementar `pdf-parse` para extraer texto de ofertas de empleo en formato PDF.
    -   Hecho 2026-08-28 (`docs/pdf-integration.md` aprobado): `src/pdf/` — `extractPdfText` con límites (10 MiB, 50 páginas, 1 MiB de texto, 20 s, 512 MB) y `worker_threads` aislado (`worker.mts` ESM autocontenido, cargado nativamente por Node en desarrollo y compilado en `dist/`); `pdfjs-dist` 6.2 endurecido (sin fuentes ni render; pdf.js ≥ 5 ya no evalúa código). `readOfferText` acepta `.pdf` en `generate-cv -f` y `analyze-offer`.
-   [x] **T-2.6: [RENDER] Salida PDF (propuesto 2026-08-28).** Renderer PDF a partir de `profile.json`.
    -   Hecho 2026-08-28 (`docs/pdf-integration.md` §3): `src/renderers/pdf/` — `renderPdfCv` con `pdfkit` desde el mismo `CvView` que Markdown (Markdown en línea → runs vía mdast), fuente OFL Source Sans 3 embebida (`templates/fonts/`), salida reproducible (fecha de creación fija = `meta.updatedAt`), paginación automática. CLI: `--format md|pdf` (`.pdf` con 0600; `--stdout` y `--template` solo con md). Aceptación: *round-trip* CV → PDF → texto (T-2.5) reproduce un golden; sin `/JavaScript` ni acciones automáticas.
    -   Nota de decisión conjunta con T-2.5 en `docs/pdf-integration.md` (2026-08-28, pendiente de aprobación): `pdfkit` desde el modelo de vista con fuente embebida; Pandoc y Puppeteer descartados; verificación por round-trip con T-2.5.

### Hito 4: Co-piloto de Inteligencia Artificial (ordenado por el Director de Ingeniería el 2026-08-28; antes «Matchmaking asistido por LLM», propuesto como Hito 3 y renumerado) — COMPLETADO Y DECLARADO por el Director el 2026-08-29 (`improve` refina el contenido, `summarize` la narrativa, `suggest tags` la estructura)

Visión y restricciones: `docs/arquitectura.md` §3 (anotaciones canónicas) y `docs/llm-integration.md` (diseño y cánones C1–C11, APROBADA Y CANONIZADA). Todo egreso de red es opt-in explícito; por defecto, modelos locales; la IA sugiere, el usuario decide.

-   [x] **T-4.1: [DESIGN] Propuesta de diseño técnico y principios para la integración de LLM.**
    -   Hecho 2026-08-28: `docs/llm-integration.md` (PROPUESTA v1, pendiente de aprobación): caso de uso principal `cv improve` (+ `summarize`, `suggest tags`), cánones C1–C10, arquitectura (CLI, flujo con `redact` y fichero de revisión, proveedor abstracto sobre REST sin SDKs, local por defecto en loopback), seguridad y privacidad (claves `CHAMELEON_*`, lista blanca de hosts, seudonimización, entradas hostiles), plan T-4.2–T-4.7 y B-5.
-   [x] **T-4.2: [LLM] `LlmProvider`, proveedores locales, `redact`, `cv llm status` y spike con modelo real** (antes T-4.1 «Abstracción `LlmService`»).
    -   Hecho 2026-08-28: `src/llm/` (HTTP JSON contenido solo loopback sin SDKs; contrato `LlmProvider`; proveedores `ollama` nativo y `openai-compatible`; configuración `CHAMELEON_LLM_*`; `llmStatus`; tarea `improve` con prompt versionado `prompts/improve.v1.md`, JSON Schema derivado de zod y validación) y `src/core/llm/redact.ts` (seudonimización: nombre, empresas opcionales, emails, teléfonos, URLs; nombres propios con grafía exacta). CLI `cv llm status`. Spike real (`npm run llm:spike`) con Qwen2.5-7B-Instruct Q4_K_M en `llama-server` (CPU): flujo completo, JSON válido, determinista con semilla, 24–35 s por logro; hallazgos: el modelo añade hechos no numéricos → el verificador C2 (T-4.3) debe cubrir entidades y términos; Ollama nativo verificado contra doble local, pendiente de Ollama real.
-   [x] **T-4.3: [LLM] `cv improve`** con verificador «sin invención» y fichero de revisión (absorbe la parte de lenguaje de la antigua T-4.4).
    -   Hecho 2026-08-28: `src/core/llm/verify.ts` (verificador de integridad semántica, canon C2 extendido: `VIOLATION_C2_NUMBER_ADDED`, `_ENTITY_ADDED`, `_CONTEXT_ADDED`, `_FACT_OMITTED`, más longitud/vacío/sin cambio; tokenización y raíces sin modelo), `src/llm/cache.ts` (caché por SHA-256 canónico, ficheros 0600), `src/llm/review.ts` (fichero de revisión parseable), `src/llm/improve-batch.ts` (orquestación tolerante a fallos) y `cv improve` (selección determinista por especialidad/oferta/límites, `--only`, `--dry-run`, `--show-payload`, `--show-prompt`, `--no-cache`) + `cv llm cache clear`. Verificado con el binario compilado y Qwen2.5-7B local: el verificador rechazó las propuestas que omitían el 40 % o añadían contexto y aceptó la fiel; segunda ejecución íntegra desde caché.
-   [x] **T-4.4: [LLM] `cv summarize`.**
    -   Hecho 2026-08-28: `src/llm/tasks/summarize.ts` (fragmento textual seudonimizado del perfil filtrado, en el orden del CV, con años de experiencia calculados por código, corpus verificable y hechos clave demostrados), `prompts/summarize.v1.md`, `src/llm/summarize-run.ts` (caché + verificador C2 en modo síntesis: sin invención de cifras ni entidades; omisión medida como cobertura de hechos clave, rechazo solo si no menciona ninguno), fichero de revisión `revision-summarize-…` con cobertura por propuesta, y `cv summarize` (misma selección determinista que `generate-cv`; `--paragraphs`, `--proposals`, `--max-length`, `--dry-run`, `--show-payload`, `--show-prompt`, `--no-cache`). Preparación de la selección compartida con `improve` (`commands/selection.ts`); ids en la `StructuredView`. Canon C9 garantizado por un test transversal (`tests/cli/source-immutability.test.ts`) para todos los comandos que generan contenido. Verificado con Qwen2.5-7B local: 2 propuestas fieles aceptadas, cobertura completa, 56 s, caché.
-   [x] **T-4.5: [LLM] Proveedores remotos (OpenAI, Anthropic)** con consentimiento explícito, claves y lista blanca (antes parte de T-4.1).
    -   Hecho 2026-08-29: `src/llm/anthropic.ts` (Messages API con herramienta forzada `tool_choice` e `input_schema` = JSON Schema de la tarea), `openai-compatible.ts` reutilizado como `openai` remoto (`authorization: Bearer`), `keys.ts` (claves solo de `CHAMELEON_OPENAI_API_KEY`/`CHAMELEON_ANTHROPIC_API_KEY` o `~/.config/chameleon-cv/keys.json` con 0600 comprobado; nunca interactivas, nunca impresas), `http.ts` (política `allowsHosts`: solo https hacia la lista blanca —`api.openai.com`, `api.anthropic.com` + `CHAMELEON_LLM_ALLOWED_HOSTS`—, sin redirecciones), `config.ts` (`selectProvider`: local por defecto; un remoto solo con `--provider` explícito, jamás vía entorno), `estimate.ts` (estimación de tokens y aviso de coste), `cv improve/summarize --provider --model --yes` con confirmación `s/N` (`consentToRemote`; sin TTY se cancela salvo `--yes`), `cv llm status [--provider]` (procedencia de claves, lista blanca, comprobación del remoto pedido). Canon C10 (verificación contextual: `policyOptions` strict/synthesis) formalizado en `verify.ts`; C11 = presupuesto. Verificado con dobles HTTP (100 % cobertura) y con el binario: rechazos sin red (sin clave, 0644, host fuera de lista, sin TTY); sin llamadas reales a OpenAI/Anthropic (sin claves; tendrían coste).
-   [x] **T-4.6: [LLM] `cv suggest tags`.**
    -   Hecho 2026-08-29: `src/core/llm/tags.ts` (diccionario cerrado = tags de las especialidades del artefacto, opcionalmente una con `-s`; política C10 de la tarea: `VIOLATION_CLOSED_DICTIONARY`, `VIOLATION_RESERVED_TAG` (`pin`), `VIOLATION_MAX_TAGS`, deduplicación y **evidencia por código** literal/contexto/inferida usando el vocabulario del perfil), `src/llm/tasks/suggest-tags.ts` (fragmento seudonimizado: texto, tags actuales, contexto del contenedor, especialidades y diccionario; JSON Schema con `enum` del diccionario; `prompts/suggest-tags.v1.md`), `src/llm/suggest-tags-run.ts` (lote con caché) y `cv suggest tags [texto]` (`-`=stdin con sus `#hashtags` como tags actuales; `--only`, `--untagged`, `--max-tags`, `--max-items`, `--explain`, `--dry-run`, `--show-payload`, `--show-prompt`, `--no-cache`, `--provider/--model/--yes`): stdout = `#tag1 #tag2` (con `<id>:` para logros del perfil), todo lo demás por stderr. C9 cubierto por el test transversal; 100 % cobertura; verificación real con Qwen2.5-7B local (véase `docs/llm-integration.md` §9).

### Hito 4.5: Consolidación y cierre de ciclos (ordenado por el Director de Ingeniería el 2026-08-29) — COMPLETADO Y DECLARADO por el Director el 2026-08-29

Elimina la fricción del flujo `improve → revisión → edición manual` sin renunciar a los cánones: aplicar es una acción explícita del usuario, mínima, localizable, con copia de seguridad y guardada por hash.

-   [x] **T-4.7: [CLI] `cv improve apply <revisión>`**: lee el fichero de revisión de `improve` o `summarize`, aplica solo las propuestas marcadas con `[x]`, identifica el fichero fuente por la cabecera de la revisión, aborta si el original cambió desde entonces (hash), crea `<fichero>.bak` antes de escribir y, opcionalmente, elimina la revisión aplicada.
    -   Hecho 2026-08-29: la revisión registra ahora la procedencia (`- fuentes: data/sources` en la cabecera; `Fuente: <fichero>:<línea> · sha256 <huella>` por ítem, calculada al generar con `loadDataset` + `resolveProvenance`; `src/cli/commands/sources.ts`), con avisos si no puede (fuentes inválidas, artefacto obsoleto, resumen orientado solo a una oferta). `parseReview` (`src/llm/review.ts`) lee el fichero editado (marcas `[x]`/`[X]`, texto retocado, párrafos, CRLF). `src/parsers/markdown/apply.ts`: cirugía mínima (tramo exacto del texto del logro sin su cola de `#hashtags`; resumen = cuerpo anterior al primer encabezado, con inserción si no existe). `cv improve apply` (`src/cli/commands/apply.ts`): una propuesta por ítem, ruta de fuente contenida (sin `..` ni absolutas), localización por texto + huella, todo-o-nada por ejecución, copia `<fichero>.bak` nunca sobrescrita (`.bak.1`…; el compilador ignora `*.bak`), escritura 0600, `--dry-run`, `--delete-review`, `-d`; opciones posicionales en `improve` para que `apply` conserve `--dry-run`/`-d`. 100 % cobertura; verificación real con el binario y Qwen2.5-7B (§9 de `docs/llm-integration.md`).
-   Tareas antiguas cubiertas por el Hito 2 sin modelo: extracción de requisitos (T-2.1), perfil a medida y adecuación (T-2.2/T-2.3), `cv match` = `cv analyze-offer` + `cv generate-cv -f` (T-2.4). La ingestión desde URL pasa al backlog (B-5).

### Hito 2.5: Consolidación y calidad de vida (ordenado por el Director de Ingeniería el 2026-08-28; `docs/consolidacion.md`)

-   [x] **T-2.7: [CLI] `cv build` unificado (ref. B-1).** La puerta de calidad del perfil, el `tsc` de los datos: estricta, silenciosa, primer paso de cualquier CI.
    -   Hecho 2026-08-28: `src/cli/commands/build.ts` — `cv build [-d] [-o] [--check] [-v]` (`build-profile` queda como alias); `--check` no escribe y falla si las fuentes tienen problemas o el artefacto falta o no está al día (comparación semántica del contenido, no por fechas); `--build` en `generate-cv` y `analyze-offer` recompila antes de trabajar. Mensajes y docs apuntan a `cv build`.
-   [x] **T-2.8: [CLI] `cv init` (ref. B-2).** Crear la estructura de directorios y los ficheros de ejemplo para arrancar un perfil nuevo.
    -   Hecho 2026-08-28: `src/cli/commands/init.ts` — `cv init [dir] [--template <dir>]` crea `data/sources/` desde el dataset distribuido en `templates/dataset/` (copia limpia de la fixture, con README para el usuario; un test comprueba que compila) y un `.gitignore` (`data/dist/`, `output/`) si no existe; nunca sobrescribe (lista conflictos, código 2); ficheros 0600; imprime los siguientes pasos.
-   [x] **T-2.9: [CORE] Sintaxis de anclaje `#pin` (ref. B-3).** Fijar logros o ítems cruciales en cualquier oferta, por encima de los algoritmos de recorte.
    -   Hecho 2026-08-28: tag reservada `pin` (`PIN_TAG`/`isPinned` en el esquema; rechazada como id o tag de especialidad). Selección: coincidencia explícita con cualquier especialidad (razón `pinned`, arrastra contenedor). Orden: anclados primero, luego puntuación, luego documento (reordenación por oferta y ranking del recorte). Recorte: nunca se recorta, consume plaza. Puntuación: no puntúa ni entra en el vocabulario. Docs precisadas en `selector-engine.md`, `scoring.md`, `trimming-cli.md`, `formato-dataset.md` y README.

### Hito 3: Representación Profesional (ordenado por el Director de Ingeniería el 2026-08-28; objetivo: Typst como motor PDF de calidad editorial)

-   [x] **T-3.1: [RESEARCH] Investigación y PoC de la integración con Typst.** Gestión del binario, interfaz y seguridad, lenguaje de plantillas, rendimiento y recomendación.
    -   Hecho 2026-08-28: `docs/typst-integration.md` (PROPUESTA v1) con evidencia ejecutada sobre Typst 0.15.1: stdin→stdout sin ficheros intermedios, `--root` verificado, hallazgo de descarga de paquetes `` y su interruptor de red (proxy), PDF determinista y etiquetado, round-trip idéntico al golden de pdfkit, 32 ms/29 MB por CV. PoC reproducible en `docs/poc/typst/` (`cv.typ` sobre `CvView`). Recomendación: motor opcional `--engine typst` sobre el binario oficial contenido; sin descarga en `npm install` (`cv typst install` explícito con SHA-256 fijados).
-   [x] **T-3.2: [RENDER] Motor Typst contenido.** `StructuredView` compartida, `source.ts`, `engine.ts`, `renderTypstCv`, `--engine pdfkit|typst`, tests al 100 % y round-trip.
    -   Hecho 2026-08-28: `src/renderers/structured/` (`buildStructuredView`, consumida ya por pdfkit sin cambiar un byte: golden intacto); `src/renderers/typst/` (`source.ts` literal + documento de dos líneas; `engine.ts` localización --typst-path → CHAMELEON_TYPST → caché → PATH, versión fijada 0.15.1, `execFile` sin shell con entorno vacío + interruptor de red por proxy + paquetes inexistentes + fuentes propias + 20 s/32 MiB + SIGKILL; `renderTypstCv` con resultado tipado); `templates/typst/cv.typ`; CLI `--engine`, `--typst-path`, `--typst-any-version`, `-t x.typ`. Tests: lógica al 100 % con runners simulados y binarios de prueba; con el binario real (`CHAMELEON_TYPST`) round-trip idéntico al golden de pdfkit, determinismo, PDF etiquetado, sondas de contención (escape del root, paquetes, tiempo agotado). Hallazgo: Typst corta por sí mismo los bucles infinitos.
-   [x] **T-3.3: [CLI] `cv typst install|status`.** Descarga explícita del release oficial con SHA-256 fijados en `src/typst/releases.json`.
    -   Hecho 2026-08-28: manifiesto `src/typst/releases.json` (8 plataformas, URL oficial, SHA-256 y tamaño calculados descargando los assets; validado con zod al cargarlo); `download.ts` (solo https, límite de tamaño, tiempo acotado, temporal 0600, SHA-256 en streaming; un hash distinto elimina el fichero); `extract.ts` (`tar` del sistema, proceso contenido); `install.ts` (atómico: descarga → extracción temporal → `--version` → renombrado; sin restos en cualquier fallo; `--force`); `status.ts` (candidatos por prioridad, versión, utilizable); CLI `cv typst install|status`. Tests al 100 % con servidor http local, archivos `.tar` reales y caché aislada; verificado con descarga real del release oficial.
-   [x] **T-3.4: [RENDER] Diseño tipográfico de `templates/typst/cv.typ`** y documentación de usuario.
    -   Hecho 2026-08-28: plantilla de referencia rediseñada (paleta y escala declaradas; versalitas espaciadas sobre regla fina; fechas alineadas a la derecha en la línea del título; ubicación e impacto en gris; tabla de skills; títulos pegajosos; pie «nombre · n / total» solo con varias páginas; sin justificar ni partir palabras), iterada revisando el render a PNG. Aceptación: golden propio `tests/fixtures/golden/cv-backend.typst.txt` + equivalencia de palabras con el golden de pdfkit + paginación con pie. Documentación: README (porqué, cómo, personalización), `docs/plantillas-typst.md` (contrato de datos, contenedor, iteración, ejemplo mínimo).

### Backlog (mejoras registradas)

-   [x] **B-1: [CLI] `cv generate-cv --build`.** Recompilar el artefacto antes de generar, como atajo explícito del flujo `build → generate-cv` (registrado 2026-08-28). → Entregado en T-2.7.
-   [x] **B-2: [CLI] `cv init`.** Copiar el dataset de ejemplo a `data/sources/` para arrancar un perfil nuevo (registrado 2026-08-28). → Entregado en T-2.8.
-   [x] **B-3: [CORE] Tag reservada `#pin`.** Fijar un ítem en cualquier oferta, no solo por especialidad (registrado 2026-08-28 en `docs/trimming-cli.md` §3.3). → Entregado en T-2.9.
-   [x] **B-4: [RENDER] Motor PDF opcional Typst.** `--engine typst` con plantilla propia cuando el binario esté instalado; máxima calidad tipográfica sin tocar el núcleo (registrado 2026-08-28 en `docs/pdf-integration.md` §3.3). → Promovido a Hito 3 (T-3.1 investigada; T-3.2–T-3.4 propuestas).
-   [x] **B-6: [QA] ESLint (typescript-eslint) como segunda puerta estática** junto a `tsc` estricto (registrado 2026-08-29 al corregir el paso `lint` inexistente de los workflows de T-6.3). → Entregado en T-9.3 el 2026-08-31 (`eslint.config.mjs`, `npm run lint`, paso en CI y en la verificación de la release).
-   [x] **B-7: [RELEASE] `strip` del ejecutable SEA** (baja prioridad, tras el Hito 7; decisión del Director del 2026-08-29): con el Node oficial el ejecutable pesa 160 MB frente a 72 MB con un Node *stripped*; investigar si `strip` conserva el blob SEA y reduce el archivo de release y la imagen Docker. → **Medido y cerrado como «no procede»** en T-9.4 el 2026-08-31: `strip` ahorra 0,3 MB de 74 (0,4 %) y rompe el ejecutable (SIGSEGV); el peso es el propio Node y nuestro blob, no los símbolos.
-   [x] **B-8: [IMPORT] Las viñetas de Word rompen la detección de columnas** — **ARREGLADA el 2026-08-31**: `isBulletCell` reconoce la viñeta (símbolos habituales y área de uso privado), `pageLines` la normaliza a «•» y `detectColumns` cuenta como fila de tabla la línea de varias celdas que empieza por una. De paso, «Empresa: Puesto» con dos puntos se lee en el orden correcto. Medido sobre el corpus del PO: `Nuevo Curriculum Lucas.pdf` pasa de **9 experiencias · 22 avisos · 8 sin situar** a **12 · 17 · 3**, y `CV Lucas.pdf` de **9 experiencias · 12 avisos** a **11 · 8**. Arnés determinista 202/202 sin mover un golden. (registrado 2026-08-31 con el corpus de cinco CV reales del PO; antes descrito como «la fecha en la columna izquierda»). **Causa raíz**: `detectColumns` descarta la hipótesis de dos columnas cuando la mitad de las líneas de la izquierda parecen filas de tabla (`rowBound`), pero no reconoce como tales (a) la celda cuya única letra es **U+F0B7**, el *bullet* del área de uso privado que emite Word con Symbol/Wingdings, ni (b) las filas de varias celdas que empiezan por `•`. En `Nuevo Curriculum Lucas.pdf` eso da **5 filas reconocidas de 28 (0,18 frente al umbral 0,50)**: la página se parte en dos columnas inexistentes, las fechas se separan de su contenido y salen **10 experiencias sin empresa, 3 descartadas enteras, 8 formaciones sin centro** (22 avisos). Arreglo: contar como fila de tabla la línea de varias celdas cuya primera celda es una viñeta —incluidas las del área de uso privado— o queda vacía al quitarla.
-   [x] **B-9: [IMPORT] Los glifos acentuados pintados sobre el hueco acaban al final de la celda** — **ARREGLADA el 2026-08-31**: en `pageLines`, un item que empieza y acaba dentro del tramo del anterior es un glifo superpuesto, no la celda siguiente; se inserta en el hueco que le corresponde según su `x` y solo si ahí hay un espacio que ocupar. `Nuevo2 Curriculum Lucas.pdf` pasa de `headline: Lucas Nunzi L pezó` y «Nombre pendiente» a **`fullName: Lucas Nunzi López`**. (registrado 2026-08-31). **Cerrada por error como artefacto de sesión y REABIERTA el mismo día**: el fichero que falla no es `Nuevo Curriculum Lucas.pdf` sino **`Nuevo2 Curriculum Lucas.pdf`** (nombres casi idénticos), que reproduce el informe del PO al pie de la letra —`headline: Lucas Nunzi L pezó`, «Nombre pendiente», 5 exp · 6 form · 37 hab—. **Causa raíz medida**: ese PDF escribe el texto con un hueco y pinta el acento como item aparte SUPERPUESTO dentro del anterior (`x=56.8 w=89.7 "Lucas Nunzi L pez"` y `x=125.4 w=5.4 "ó"`, es decir 125.4 cae dentro de 56.8–146.5); `pageLines` ordena por `x` y lo concatena al final. Arreglo: un item que empieza dentro del tramo del anterior no es la celda siguiente sino un glifo superpuesto, y va insertado en su posición — `(125.4−56.8)/89.7 × 17 = 13`, que es exactamente el espacio entre «L» y «pez»: sustituirlo da `Lucas Nunzi López`.
-   [x] **B-14: [LLM] El verificador C2 daba por inventada una cifra que sí estaba** — **ARREGLADA el 2026-09-01**, y el hallazgo salió de que el PO probara `summarize` con su perfil real: le rechazó las dos propuestas y me lo enseñó. **No era el modelo inventando: era nuestro código.** `tokenize` troceaba «21 709» en dos números y `classify` no canonizaba los separadores, así que «21.709», «21 709» y «21709» eran cifras distintas y cualquiera con separador contaba como añadida. Con un perfil lleno de cifras eso rechaza casi todo. Arreglado en la raíz (una cifra vale por su valor; el separador solo se retira si agrupa de tres en tres, para no romper «8.3» ni «1,4»), con prueba de que una cifra realmente nueva se sigue rechazando.
    -   **Segundo defecto, de usabilidad**: el informe escribía el detalle del rechazo pero `parseReview` no lo recuperaba —cerraba la propuesta al llegar a `- motivo:`, que va antes—, así que la web solo podía decir «no supera la verificación». Ya se lee y se muestra: sin ese detalle no se distingue una invención del modelo de un dato que falta en las fuentes.
    -   **`--allow-new-numbers`** (`improve` y `summarize`): admite cifras sin respaldo bajo la responsabilidad de quien lo pide, avisando de cada una. Por defecto NO. **Decisión tomada tras medir**: el PO pidió «que por defecto se eliminen esas cifras», pero con el defecto arreglado el comportamiento por defecto ya es ese; lo que faltaba era la salida explícita en el otro sentido. Se descartó recortar la frase que contiene la cifra: «0 divergencias en 2 412 casos» sin la cifra queda mutilada, y mutilar es peor que rechazar.
-   [x] **T-9.22: [IMPORT] Importador del MAC de Manfred (JSON)** — **ENTREGADA el 2026-09-02** (`docs/cv-import.md` §12). Encargo del PO, con su propio MAC como caso de prueba. El MAC («Manfred Awesome CV») trae los datos ESTRUCTURADOS, así que comparte camino con la exportación de LinkedIn (§8): se rellena el mismo `DraftProfile` y todo lo de aguas abajo —validación entidad a entidad, borrador, informe, duplicados, adopción— sirve sin cambios; **nada queda «sin situar»**. `cv import-manfred`, `POST /import-manfred` y la opción del selector «Origen» en la web. Tres decisiones que no son obvias: `location.notes` se descarta (Manfred guarda ahí la traza de su autocompletado de Google Maps, que no es una ubicación sino cómo se obtuvo); un empleo con varios roles son **varias entradas**, porque en el perfil cada puesto tiene sus fechas y sus logros y fundirlos perdería la promoción; y las habilidades duras entran **sin categoría** —el `type` de MAC («technology») no es ninguna de las del perfil—, mientras que las blandas sí van a `soft`, donde la equivalencia es exacta. Lo que un MAC guarda y el perfil no (puestos buscados, contrato, salario, estado, recomendaciones, artefactos públicos) **no se calla ni se le busca hueco**: encabeza el informe como «no importado». Dos avisos salieron del fichero real: varios estudios con la MISMA fecha de inicio y ninguno terminado —la forma de rellenar la formación de una sentada; se avisa pero la fecha NO se toca— y una ubicación que se queda en el país, que acabará en el campo de ciudad. Tolerante y sin red: el `$schema` no se descarga, otra versión se avisa y se importa igual, y solo se exige que el fichero SEA un MAC. Medido sobre `my-mac-from-manfred.json`: 6 experiencias, 3 formaciones, 41 habilidades, 2 idiomas, **0 degradado y 0 sin situar**, y `cv build --data` en verde.
-   [x] **T-9.21: [IMPORT] Elegir la carpeta a importar sin escribir su ruta** — **ENTREGADA el 2026-09-02** (`docs/cv-import.md` §10.5). Encargo del PO: «cuando selecciono carpeta quiero poder seleccionar la carpeta sin tener que escribir la ruta». **Un selector de directorio del navegador no sirve**, y conviene dejarlo escrito: la web entrega los ficheros de la carpeta elegida pero NUNCA su ruta en el disco, y esta importación la hace el servidor leyendo el espacio de trabajo —lo que permite la tabla comparativa y lo que la iguala con `--all` (C14)—. Así que las ofrece quien conoce las rutas: `GET /api/v1/import-cv/folders` recorre el espacio y devuelve las carpetas **con CV dentro y cuántos**, de modo que se elige sabiendo lo que se importaría y no se puede pedir una que no traería nada. Tres niveles y 400 carpetas como tope; fuera `node_modules`, ocultas, `.bak` y las del producto (`import/`, `data/`, `output/`). Con una sola candidata queda puesta. La lista no es una jaula: el campo de ruta sigue disponible y la pantalla cae a él sola si el recorrido falla. Verificado sobre el espacio real del PO: una candidata, `cv-corpus` con 6 CV.
-   [x] **T-9.20: [FUENTES] Duplicados en las propias fuentes: detectarlos y resolverlos** — **ENTREGADA el 2026-09-02** (`docs/cv-import.md` §11). Encargo del PO tras usar T-9.19: «necesito una herramienta para detectar duplicados en mis fuentes y poder solventarlos». Adoptar entrada a entrada de varios borradores es justo lo que los crea. `cv duplicates` (list, resolve), la pantalla **Duplicados** y las rutas `GET /duplicates` y `POST /duplicates/resolve`. **Detectar no es un criterio nuevo**: el agrupado sale de `drafts.ts` a `src/app/duplicates.ts` —qué es «la misma cosa» no es de los borradores— y se aplica a `data/sources` contra sí mismo. Lo que importa es **lo que NO marca**: un empleo contado por periodos comparte empresa pero no fechas; medido sobre el espacio de trabajo real, de 22 entradas salen 2 grupos y **cero** falsos positivos sobre las cuatro entradas de Life5 partidas por etapa, que habría sido el peor falso positivo posible. **Resolver** lo decidió el dato real: en los duplicados que salen de importar cada mitad tiene lo que a la otra le falta —una las fechas y «Centro pendiente», la otra el centro y ninguna fecha—, así que borrar cualquiera pierde información; se elige cuál se queda y absorbe de las otras SOLO lo ausente, sin pisar jamás un valor existente y diciendo qué se conserva y qué se descarta cuando difieren. «Empresa/Centro pendiente» cuentan como ausencia; el periodo va entero porque un `end` vacío significa «en curso»; las listas se añaden sin repetir y los logros con id libre; el perfil resultante se valida entero antes del disco. **Deshacer por el histórico de fuentes**, no por copias nuevas: para eso hizo falta arreglar `restoreSourceVersion`, que fallaba si la fuente ya no existía y dejaba sin vuelta cualquier borrado.
-   [x] **T-9.19: [IMPORT] Ver, editar y adoptar los borradores** — **ENTREGADA el 2026-09-02** (`docs/cv-import.md` §10). Encargo del PO: «he importado varios cvs pero no veo que se añada información sobre los currículums, experiencias». El producto hacía lo que decía —`import-cv` deja un **borrador** y nunca escribe en `data/sources/`, D2— pero faltaba la vuelta del camino: no había forma de volver a un borrador, revisarlo y llevárselo. Ahora `cv drafts` (list, show, duplicates, adopt), la pantalla **«Borradores»** y las rutas `GET /drafts`, `GET|PUT /drafts/{nombre}/files/{ruta}` y `POST /drafts/adopt`, con el criterio entero en `src/app/drafts.ts` (C14). **Adoptar no es fusionar** y `docs/portability.md` §1 sigue en pie: se copia la entrada señalada a un fichero NUEVO con id libre, con los mismos serializadores que `cv import`, validando el perfil resultante entero antes de tocar el disco y escribiendo con la huella «*», que crea o falla —una adopción no puede pisar una fuente tuya, y deshacerla es borrar el fichero—. Solo las secciones en las que un fichero es una entrada (experiencia, formación, proyectos): habilidades y certificaciones viven en un CSV compartido y exigirían reescribir un fichero que ya es del usuario. **Los duplicados se enseñan, no se resuelven**: seis CV de la misma persona se contradicen en las fechas del mismo empleo, traen empresa y puesto intercambiados y llegan espaciados letra a letra o con mojibake, así que se agrupa y se pregunta. Cada regla de agrupado salió de un fallo medido sobre el corpus real —los «Empresa/Centro pendiente» fuera de la comparación, grupos por semejanza con la SEMILLA y no en cadena, semillas con fechas antes que sin ellas, y el solapamiento contra la media geométrica de los dos periodos en vez de contra el más corto—: de un grupo encadenado de 24 a **27 grupos, uno por empleo o titulación real** (8 borradores, 158 entradas). Límite aceptado y con prueba: una entrada sin fechas puede caer en un grupo con el que solo comparte el centro; agrupar de menos esconde duplicados y agrupar de más cuesta una mirada.
-   [x] **B-15: [IMPORT] `--replace` no sustituía el borrador, escribía encima** (encontrado el 2026-09-02 revisando por qué el corpus del PO no cuadraba con sus informes). `writeDraft` escribía el plan fichero a fichero sobre `import/<nombre>/` sin apartar lo que hubiera: una reimportación que reconoce **menos** entradas que la anterior dejaba vivas las sobrantes. Medido en el espacio de trabajo real: `import/lucas-nunzi-lopez/` con **30 ficheros de experiencia** de cuatro pasadas distintas (12 del 31-ago, 6+5 del 2-sep 14:03 y los 7 del 14:08) y un `README.md` que decía «7 experiencias»; `cv build --data` sobre ese borrador cargaba las 30. Lo grave no es el desorden, es que **el informe y el contenido dejan de coincidir**, y el borrador es justo lo que se revisa antes de adoptarlo. **ARREGLADA el 2026-09-02**: sustituir es apartar y escribir de cero, con el mismo procedimiento que `cv import --replace` (`backupDirectory` + `rename`), así que el borrador anterior queda entero en `import/<nombre>.<marca>.bak` —no se borra nada, y desde que un borrador se puede editar a mano eso es trabajo del usuario (C9)— y la carpeta nueva contiene exactamente el plan de la importación. La copia se nombra en la salida de la CLI y viaja en `backup` en `POST /import-cv`.
-   [x] **B-12: [IMPORT] La duración de LinkedIn se tomaba por el título** — **ARREGLADA el 2026-08-31** con `Profile.pdf` (el «Guardar como PDF» del perfil, sexto CV del corpus): «noviembre de 2016 - abril de 2017 (6 meses)» dejaba «(6 meses)» como puesto. Se retira la duración entre paréntesis; «(remoto)» y demás se respetan.
-   [x] **B-14: [IMPORT] Un nombre espaciado letra a letra bautiza el borrador con una sílaba** (encontrado el 1-sep al revisar el corpus con el binario 1.15.0). `Lucas Nunzi López.pdf` maqueta la cabecera con espaciado por carácter y el borrador sale con `fullName: L U C A S`, `headline: N U N Z I` y la carpeta `import/l-u-c-a-s/`. El producto **ya sabe** reconocer ese espaciado —`isSpacedHeading` lo usa para los títulos de sección desde B-10—, pero `chooseName` no lo colapsa antes de puntuar los candidatos. Arreglo previsto: colapsar el espaciado en el candidato antes de `nameScore` (y solo ahí: el cuerpo del CV no se toca), con la misma guarda de B-10 para no destrozar siglas. Coste estimado: pequeño. **Las otras cinco entradas de ese mismo borrador salen bien**, así que es la cabecera y nada más. **ARREGLADA el 1-sep**: en la cabecera, un bloque de líneas espaciadas seguidas se colapsa **línea a línea** —el salto de línea es lo único que conserva la frontera entre palabras, que dentro de una línea espaciada se pierde sin remedio (B-10)— y se prueban todos los tramos de dos líneas o más, quedándose con el que mejor puntúa como nombre; a igualdad, el más largo y, si aún empatan, el primero. Así «D E S A R R O L L A D O R W E B», que viene detrás y también espaciado, no se cuela: al añadirlo el nombre pasa de tres palabras a cuatro y puntúa menos. Y un rótulo espaciado que preceda al nombre («C U R R I C U L U M» / «V I T A E») lo descarta el mismo diccionario de T-9.1, esté pegado al nombre o en un bloque aparte. **El titular se deja como viene**, espaciado: «DESARROLLADORWEB» parecería correcto sin serlo, y espaciado se ve que hay que repasarlo. Medido: `Lucas Nunzi López.pdf` pasa de `fullName: L U C A S` y carpeta `l-u-c-a-s` a **`LUCAS NUNZI LÓPEZ`**; los otros cinco CV del corpus dan exactamente los mismos números.
-   [x] **B-13: [IMPORT] El PDF de LinkedIn no trae fechas de formación ni ordena empresa/puesto como el resto** (registrado 2026-08-31 con `Profile.pdf`). Dos límites distintos: (a) su sección «Educación» lista pares centro/titulación **sin ninguna fecha**, y las entradas abren por fecha, así que no abre ninguna —seis líneas sin situar—; inventar una fecha está descartado; (b) pone la **empresa antes que el puesto**, al revés de «Rol · Empresa», y aquí no hay marca fiable que las distinga (en formación sí la hay: `I.E.S`, `Universidad`…), así que el orden no se toca. **Antes de tocar nada de esto, conviene recordar que ese mismo perfil se importa perfecto con `cv import-linkedin` desde la exportación CSV (T-9.8): datos estructurados y cero líneas sin situar.** **ARREGLADA el 1-sep** (`docs/cv-import.md` §9): lo que faltaba era mirar **el documento** en vez de la línea. Se exigen dos señales independientes —la URL `linkedin.com/in/<slug>` y el pie paginado que LinkedIn escribe siempre—, de modo que un CV que solo cite su LinkedIn no entra por aquí; verificado eso, cambian tres reglas y solo tres: (1) el nombre se identifica por el **slug de su propia URL** —comprobar, no adivinar; si no casa, se vuelve al lector general y no se inventa nada—, porque la barra lateral aplanada lo empujaba fuera de la ventana de cabecera; (2) la experiencia se agrupa en «empresa / puesto / rango», y lo que va tras la fecha es el cuerpo del empleo anterior; (3) la formación se empareja por posición **sin inventar la fecha que falta** (el esquema la admite opcional). Además «Contactar», «Aptitudes principales» y «Extracto» entran en el diccionario de títulos. **Medido sobre `Profile.pdf`**: de «Nombre pendiente», 0 formaciones, 0 habilidades, 2 empleos fundidos, 2 sin empresa, 3 avisos y 6 líneas sin situar, a **6 experiencias correctas, 3 formaciones, 3 habilidades, nombre/titular/correo/ubicación, 0 avisos y 0 sin situar**, y el borrador compila con `cv build --data`. Los otros cinco CV del corpus dan exactamente los mismos números que antes (comparación con el binario anterior). **La recomendación no cambia**: para un perfil de LinkedIn, `cv import-linkedin` con la exportación oficial sigue siendo mejor.
-   [x] **B-11: [IMPORT] Rangos de fecha que llegan sin el guion** — **ARREGLADA el 2026-08-31, y la causa no era la que parecía**. El guion sí falta en esa exportación (entre «2011» y «2013.» solo hay un item de espacio), pero lo que de verdad bloqueaba era otra cosa: la fila empieza con el **U+F0B7 de Word pegado a la fecha**, y la normalización de B-8 solo actuaba sobre una celda que fuera *solo* la viñeta. Invisible como es, rompía cualquier regla anclada al principio de la línea. Arreglado a la vez: la viñeta se normaliza esté sola o pegada, se retira donde no signifique nada, no se cuela en el título de la entrada, y se admite el rango de dos años sin separador con guarda estrecha (celda solo con los dos años, en orden y a menos de 60 de distancia; «(Curso 1819)» y «2013 2011» no entran). Medido: `CV Lucas.pdf` de **0 formaciones · 22 sin situar** a **7 · 4**. Arnés 202/202 sin mover un golden.
-   [x] **B-10: [IMPORT] CV que espacian el texto letra a letra** — **CERRADA el 2026-08-31**. `Lucas Nunzi López.pdf` pasa de **0 experiencias · 0 formaciones · 109 líneas sin situar** a **7 · 3 · 0, sin un solo aviso**. Tres arreglos, por orden de lo que pesaban:
    -   **El que más daño hacía**: la regla «una cabecera espaciada desconocida cierra la sección» (T-8.4b F2, nacida de «C A M P U S I N V O L V M E N T») disparaba con cada **nombre de empresa** espaciado —`R A I O L A N E T W O R K S`— y hasta con el cuerpo del texto, dejando el CV entero sin situar. Ahora se decide por documento: si espaciar es el ESTILO de la plantilla (≥6 líneas espaciadas y ≥8 % del total), el espaciado no dice nada sobre qué es una cabecera y la regla se apaga; si es la excepción, sigue actuando igual que antes (con prueba de las dos ramas).
    -   `collapseSpacedDigits` recompone lo recuperable sin inventar —cifras y saltos letra/cifra—, que es lo que abre las entradas.
    -   Un título de sección partido en dos líneas («E D U C A C I Ó N» / «P R E V I A») deja de tomarse por cabecera desconocida, y la formación con el centro arriba y la titulación debajo se reparte al derecho cuando una sola de las dos líneas lleva marca de centro (`I.E.S`, `Universidad`, `Escuela`…).
    -   **Límite que queda escrito, y no es nuestro**: la frontera entre palabras NO existe en lo que entrega pdf.js —el texto espaciado llega en un solo item con un único espacio entre cada letra—, así que los títulos siguen saliendo espaciados (`subtitle: R A I O L A N E T W O R K S`). Recuperarlos exigiría segmentación por diccionario, que es inventar, o leer los operadores del PDF por debajo de pdf.js: **decisión del PO si alguna vez molesta**. La entrada, sus fechas y su estructura ya son correctas.
-   [x] **B-5: [INGEST] Texto de una oferta desde URL.** Obtención y saneado del HTML (`cheerio`); egreso de red explícito; sin persistir páginas (antes T-4.2; movido al backlog por `docs/llm-integration.md` §6 el 2026-08-28). → Entregado en T-8.5 (v1.9.0), con extractor propio por JSON-LD + texto visible + `og:*` en vez de `cheerio` y guardia SSRF.

### Hito 5: Presentación Profesional y Personalización (ordenado por el Director de Ingeniería el 2026-08-29) — COMPLETADO Y DECLARADO por el Director el 2026-08-29

El *qué* (contenido) y el *cómo* (lógica) están resueltos; ahora el *aspecto*: convertir el renderizado de un motor funcional en una plataforma de diseño en la que el usuario elige cómo se presenta su perfil.

-   [x] **T-5.1: [RENDER] Sistema de temas (theming)**: `themes/<nombre>/` con `template.typ` y `theme.toml` (colores, tipografías, tamaños, márgenes) leído y validado; `generate-cv --theme <nombre>` (por defecto `default`), búsqueda en `themes/` del proyecto; el `TypstRenderer` recibe la configuración del tema y la ruta de su plantilla; entregable inicial = `cv.typ` actual refactorizado como `themes/default/`.
    -   Hecho 2026-08-29: `src/themes/` (`schema.ts`: `theme.toml` → TOML (`smol-toml`, sin dependencias) → zod estricto con rutas en los errores: colores `#rrggbb`, fuentes, tamaños 4–72 pt, espaciados en em, papel de una lista y márgenes en mm; `loader.ts`: `themes/` del proyecto con el FS inyectado y después los distribuidos, nombre validado antes de tocar el disco, `theme.name` debe coincidir con el directorio, `fonts/` opcional, lista de disponibles en el error). `themes/default/` = `cv.typ` refactorizado: `styles(theme)` convierte las variables a unidades Typst y las piezas las reciben; el golden del binario real es idéntico. El tema viaja a Typst como segundo literal JSON (`#cv(vista, tema)`; contrato `cv(d, theme)` también para `-t`), el `--root` es el directorio del tema y `--font-path` se repite con `fonts/` del tema. `generate-cv --theme` (solo con `--engine typst`; tema inválido = código 1 con la ruta del error). 100 % cobertura; 9/9 pruebas con el binario real.
-   [x] **T-5.2: [RENDER] Segundo tema `classic` y anulaciones de configuración (`cv.toml`)**: tema serif, sobrio y de aire académico, distinto del `default`; fichero opcional `cv.toml` en la raíz del proyecto cuya sección `[theme]` anula, solo para esa ejecución, valores del `theme.toml` del tema elegido (fusión en cascada validada con el mismo esquema zod).
    -   Hecho 2026-08-29: `themes/classic/` (Libertinus Serif embebida en Typst; cabecera centrada en versalitas bajo doble filete; secciones en mayúsculas con filete; puesto en negrita y empresa en cursiva; cuerpo justificado con partición; viñetas con raya; revisado en PNG). Hallazgo: un `tracking` alto en mayúsculas hace que pdf.js —y cualquier ATS— extraiga «E X P E R I E N C I A»; se fijó en 0.05em y la suite exige con el binario real que `classic` conserve exactamente las palabras del golden. `src/themes/project-config.ts`: `cv.toml` (`[theme]` con `name` y las tablas de `theme.toml` como parciales estrictos derivados del mismo esquema; `applyThemeOverrides` fusiona en cascada y revalida; `overriddenKeys` para `--explain`); `generate-cv`: `--theme` > `cv.toml [theme].name` > `default`, `cv.toml` inválido = código 1 con ruta del error antes de renderizar, ignorado con pdfkit/Markdown. 100 % cobertura; 10/10 pruebas reales.
-   [x] **T-5.3: [CLI] `cv theme list | path <nombre> | create <nombre>`**: gestión del ecosistema de temas (inventario con origen y descripción, ruta absoluta de un tema, andamiaje de un tema nuevo en `themes/<nombre>/` a partir de `default`), con el sistema de ficheros inyectado y errores claros.
    -   Hecho 2026-08-29: `src/themes/loader.ts` (`locateTheme` sin validar, `inventoryThemes` con origen, descripción, validez y ocultación; `LoadedTheme.root`) y `src/cli/commands/theme.ts`: `list` (proyecto primero; marca el tema por defecto según `cv.toml`; los inválidos aparecen con su motivo; `cv.toml` inválido solo avisa), `path` (ruta absoluta; si el tema existe pero no es utilizable, la imprime con aviso: la ruta sirve para arreglarlo), `create` (`--from <tema>`, por defecto `default`; `theme.toml` con `name` reescrito, `template.typ` y `fonts/` si los hay; nombre validado; nunca sobrescribe; avisa si oculta a un distribuido; 0644). 100 % cobertura; probado con el binario.

### Hito 5.5: Validación Integral (ordenado por el Director de Ingeniería el 2026-08-29; prioridad número uno) — COMPLETADO Y DECLARADO por el Director el 2026-08-29 (cánones C12 y C13)

Brecha detectada tras el Hito 5: cada característica tiene sus pruebas unitarias y de integración (100 % de cobertura), pero no existía una **prueba de aceptación integral** del producto de principio a fin. Estrategia aprobada y canonizada por el Director: **validación de dos niveles** —un arnés **determinista**, rápido y sin red, que exige coincidencia perfecta con salidas esperadas; y un arnés de **IA** que, contra un modelo local real (Ollama) y sin juzgar el texto exacto del modelo, valida la integridad estructural y la consistencia semántica del proceso—. Ambos operan sobre un único banco de pruebas y nunca escriben en él (canon C9): cada ejecución trabaja sobre una copia temporal. La finalización de este hito es la puerta de entrada para reanudar el Hito 6.

-   [x] **T-5.5.1: [QA] Banco de pruebas (test bench)**: `tests/acceptance/bench/` como única fuente de verdad de las pruebas de aceptación, con datos **sintéticos** (ninguna PII real): un dataset de perfil realista y complejo (varias especialidades con resumen y tags, experiencias y proyectos con logros etiquetados, con `impact`, ids explícitos, Markdown en línea y `#pin`, skills con alias, certificaciones, formación, idiomas; y un perfil largo que pagine), un `cv.toml` y un tema propio (para validar la cascada de temas), varias ofertas de empleo en **texto y en PDF** (los PDF generados de forma reproducible por un script del propio banco, no descargados), revisiones de `improve`/`summarize` marcadas a mano (para validar `apply` de forma determinista) y los **artefactos esperados** de los flujos deterministas (Markdown, PDF de pdfkit y de Typst con `default`, `classic` y el tema propio, informes de `analyze-offer`, artefacto `profile.json`, fuentes tras `apply`), regenerables solo de forma deliberada (`--update`) y revisables en el diff.
    -   Hecho 2026-08-29: `tests/acceptance/bench/workspace/` (persona sintética con once años de trayectoria: 4 especialidades, 5 experiencias y 3 proyectos —29 logros con `impact`, ids explícitos, Markdown en línea y un `#pin`—, 16 skills con alias, 4 certificaciones, 2 formaciones, 3 idiomas; 4 ofertas en texto, 2 de ellas en PDF; tema `bench` derivado de `classic`; `cv.toml` con tema por defecto y anulaciones; 2 revisiones marcadas), `bench/generate.ts` (`npm run acceptance:bench`: PDF de ofertas con pdfkit y fecha fija, revisiones con `Fuente:` y huella sobre las fuentes; reproducible byte a byte), `cases.ts` (catálogo declarativo: 6 escenarios, 72 pasos con código de salida esperado y ficheros producidos) y `runner.ts` (`npm run acceptance:update`: binario compilado sobre una copia temporal, entorno mínimo determinista, rutas normalizadas) que generó `bench/expected/` (260 artefactos, 1,4 MB: Markdown, `profile.json`, 3 PDF de pdfkit y 6 de Typst con `default`, `classic`, `bench` y un tema creado, informes de `analyze-offer` en texto y JSON, vista previa seudonimizada del co-piloto, fuentes tras `apply` con `.bak`). Dos pasadas completas producen artefactos idénticos; los CV del banco paginan (3 páginas con pdfkit, 2 con Typst). `tests/acceptance/README.md`.
    -   Corrección 2026-08-29 (detectada al provocar fallos en T-5.5.2): los artefactos esperados bajo `files/output/` y `files/data/dist/` estaban **ignorados por el `.gitignore` de la raíz** (`output/`, `data/dist/`, `dist/` sin anclar) y no habían entrado en git; los patrones se anclan ahora a la raíz (`/output/`, `/data/dist/`, `/dist/`), los 100 ficheros que faltaban se versionan y el arnés comprueba en cada ejecución que ningún artefacto esperado quede ignorado por git.
-   [x] **T-5.5.2: [QA] Arnés de aceptación determinista**: `npm run test:acceptance:deterministic`, un script que ejecuta el **binario compilado** (`dist/index.js`, proceso hijo real: sistema de ficheros real, códigos de salida reales) sobre una copia temporal del banco y recorre los flujos clave —`init`, `validate`, `build` (y `--check`), `generate-cv` en Markdown y PDF con `-s`, `-f/--from-job-offer` («match-offer»: ofertas en texto y PDF), `--compact`, límites, `--explain`, `--engine typst` con `--theme` y `cv.toml`, `analyze-offer` (texto y `--json`), `theme list|path|create`, `improve apply` sobre las revisiones marcadas, `llm status` y `typst status` sin servicios— y compara cada salida con la esperada exigiendo **coincidencia perfecta** (byte a byte en Markdown, JSON y PDF —ambos motores son deterministas—; ante una discrepancia en PDF muestra además el diff del texto extraído). Sin red; los casos de Typst se ejecutan si hay binario (caché de usuario o `CHAMELEON_TYPST`) y, si no, se marcan como **omitidos de forma visible** en el resumen, nunca en silencio. Termina con un resumen (pasadas, fallidas, omitidas, tiempo) y código distinto de cero ante cualquier fallo.
    -   Hecho 2026-08-29: `tests/acceptance/runner.ts` en modo comparación (por defecto) y `compare.ts`: por paso, código de salida (catálogo y `.exit.txt` coherentes), stdout y stderr normalizados y cada fichero producido byte a byte —texto y JSON con diff por líneas (LCS, contexto, salida acotada); PDF con bytes y, si difieren, páginas, tamaño y diff del texto extraído; árboles con ficheros que faltan o sobran—; comprobación previa de que `generate.ts` reproduce los ficheros derivados versionados; `dist/` obligatoriamente al día con `src/`; omisión visible del escenario `typst` sin binario (`--require-typst` la convierte en fallo); `--update`, `--keep` y filtro por escenarios; resumen final y códigos 0/1/2. Verificado en verde (7 escenarios, 76 pasos, 32 s) y en rojo con diferencias provocadas en Markdown, PDF y código de salida. Pruebas unitarias de los comparadores.
-   [x] **T-5.5.3: [QA] Arnés de aceptación de IA**: `npm run test:acceptance:ai`. **Precondición programática**: antes de nada comprueba que una instancia local de Ollama responde y sirve el modelo configurado (por defecto `qwen2.5:7b-instruct`; se respetan `CHAMELEON_LLM_*`), y si no, falla con un mensaje claro que dice qué arrancar y qué modelo descargar; nunca usa un proveedor remoto. **Flujo**: sobre una copia temporal del banco ejecuta `cv improve`, `cv summarize` y `cv suggest tags` (con `--no-cache`, para que cada ejecución sea real; con `--show-payload` para inspeccionar lo que sale). **Principio de validación**: no se compara el texto generado por el modelo; se valida la integridad estructural y la consistencia semántica del proceso: (a) las órdenes terminan sin error; (b) los ficheros de revisión (`output/revision-improve-*.md`, `revision-summarize-*.md`) existen y **cumplen el formato** (se leen con `parseReview`: cabecera con procedencia, un ítem por logro con `Fuente:`, cada propuesta con su veredicto); (c) **sin invención**: toda propuesta marcada como aceptada vuelve a superar el verificador semántico ejecutado de forma independiente contra las fuentes del banco, y toda propuesta rechazada lleva su código `VIOLATION_*`; (d) las etiquetas de `suggest tags` pertenecen **todas** al diccionario cerrado (las tags de las especialidades del banco), con la grafía del diccionario y sin `pin`; (e) lo que sale hacia el modelo no contiene PII del banco (email, teléfono, enlaces; nombre sustituido por `[NOMBRE]`); (f) nada se ha escrito en `data/sources` de la copia (C9). Informa de la tasa de aceptación y de los tiempos como datos, no como criterio.
    -   Hecho 2026-08-29: `tests/acceptance/ai-runner.ts` (precondición con `llmStatus` sobre un entorno filtrado —solo `CHAMELEON_LLM_PROVIDER/BASE_URL/MODEL`, nunca claves ni remotos—; copia temporal del banco; `improve -s backend --top-n 1 --max-items 3`, `summarize -s backend`, `suggest tags --only …` y con texto y `-s`; 16 comprobaciones: salida sin error y sin logros fallidos, formato de las revisiones (`parseReview` + líneas de verificación coherentes con el estado de cada propuesta), **re-verificación independiente** con `verifyProposal` y las políticas `strict`/`synthesis` sobre fragmentos reconstruidos desde las fuentes del banco, diccionario cerrado con grafía exacta y sin `pin`, ausencia de PII en las cargas útiles, `data/sources` intacto). Ejecutado en real con Qwen2.5-7B (llama-server, proveedor `openai-compatible`): 16/16 en 3 min 34 s (improve 3 logros · 6 propuestas · 2 aceptadas; summarize 2/2 aceptadas; suggest tags 7 etiquetas, todas del diccionario). Sin modelo: código 2 con instrucciones. Pruebas unitarias de los ayudantes.
-   [x] **T-5.5.4: [DOCS] Documentación del proceso de validación**: `docs/acceptance-testing.md`, el nuevo estándar de calidad: propósito y alcance de cada arnés, cómo ejecutarlos (compilación previa, `npm run test:acceptance:deterministic`, `npm run test:acceptance:ai`), prerrequisitos (Typst para los casos de PDF de calidad editorial; **Ollama configurado y con el modelo** para las pruebas de IA, con las variables `CHAMELEON_LLM_*`), cómo interpretar el resumen y los diffs, cómo y cuándo regenerar deliberadamente los artefactos esperados (`--update`, revisando el diff), qué valida y qué no valida la prueba de IA, y el encaje en el flujo de release del Hito 6 (el arnés determinista, en el trabajo `verify` de cada release; el de IA, ejecución manual o nocturna con un modelo local).
    -   Hecho 2026-08-29: `docs/acceptance-testing.md` (propósito y alcance de los dos niveles con su tabla comparativa; el banco de pruebas; el arnés determinista: requisitos, funcionamiento, cómo leer un fallo, autocomprobaciones C13 y regeneración deliberada; el arnés de IA: precondición, qué ejecuta, las seis familias de comprobaciones y qué no valida (C12), resultado de referencia; encaje en el trabajo diario y en el release; cánones C9/C12/C13; referencia rápida). Canon C12 anotado en `docs/llm-integration.md`; README con las órdenes de aceptación y el enlace a la guía.

## Hito 6: Release y Distribución (ordenado por el Director de Ingeniería el 2026-08-29; en pausa durante el Hito 5.5; REANUDADO el 2026-08-29 con las decisiones de T-6.1 aprobadas: Node SEA + esbuild, Typst como binario externo, lanzamiento incremental sobre una plataforma de referencia, GitHub Actions) — CERRADO para la v1.0.0 el 2026-08-29 (T-6.6 aprobada; T-6.4 y T-6.5 pospuestas)

De una colección de scripts en un repositorio a un producto: un ejecutable autónomo por plataforma y un flujo de release automatizado, con la misma disciplina de cadena de suministro que el resto del proyecto (binarios oficiales, hashes, consentimiento explícito).

-   [x] **T-6.1: [RELEASE] Diseño de empaquetado y release.** APROBADA por el Director el 2026-08-29.
    -   Hecho 2026-08-29: `docs/packaging-and-release.md` (PROPUESTA v1, aprobada): inventario de lo que se empaqueta y de los ocho acoplamientos con el disco (todos `resolve(__dirname, …)`), análisis de Node SEA, `pkg`/`@yao-pkg/pkg`, `nexe`, `boxednode`, `bun`, `deno` y los canales sin binario (npm, Homebrew) con criterios de cadena de suministro, plataformas, assets, tamaño y arranque; *spike* real con Node 26 (`--build-sea`: 4,6 MB de bundle, 68 MB de ejecutable con el Node del sistema, 16 MB en xz, arranque 212 ms frente a 356 ms; `postject` sobre el Node de la distribución produce un binario que falla: usar siempre el oficial); recomendación SEA + esbuild + capa de assets sobre nuestra abstracción `FileSystem` (fuentes y dataset materializados en la caché de usuario con hash; worker de PDF por `eval`; versión inyectada), Typst fuera del binario (variante `-full` opcional); flujo de release por tag con verify → build (matriz de 5 plataformas, prueba de humo del binario) → release (`SHA256SUMS.txt`, atestación SLSA, acciones ancladas por SHA); npm como segundo canal, Homebrew después; firma comercial como decisión aparte; plan T-6.2–T-6.6 y seis decisiones para el Director.
-   [x] **T-6.2: [RELEASE] Capa unificada de assets, bundle y `npm run package`**: una sola puerta de acceso a los assets (repositorio en desarrollo, `node:sea` en el ejecutable, materialización con hash en la caché para lo que debe ser un fichero real), los ocho acoplamientos por la capa, empaquetado SEA en la plataforma de referencia (linux-x64) y aceptación con los arneses contra el binario (`--binary`). Plan de implementación en `docs/asset-layer.md` (2026-08-29), pendiente de aprobación.
    -   Hecho 2026-08-29 (S1–S6): `src/shared/assets.ts` (`AssetStore`; `DiskAssets`, `MemoryAssets`, `SeaAssets` con materialización atómica en `<caché>/assets/<versión>` y SHA-256 comprobado contra el manifiesto embebido), `src/shared/cache.ts`; `CliContext.assets` y los ocho acoplamientos por la capa (versión, plantilla Markdown, prompts, dataset de `init`, temas distribuidos, fuentes de Typst y de pdfkit —bytes—, worker de PDF por código); `src/pdf/dom-matrix.mts` (pdf.js exige `DOMMatrix` al cargarse y `@napi-rs/canvas` no viaja en el binario) e importación estática de `pdf.worker.mjs`; `scripts/package.ts` = `npm run package` (compilación limpia, esbuild con plugin para las fuentes estándar de pdfkit, manifiesto, `node --build-sea`, prueba de humo, `tar.gz` reproducible con `.sha256`: 69 MB / 23 MB, 21 s en linux-x64); `--binary` en los dos arneses con el marcador `<BUILTIN_THEMES>`. **Aceptación cumplida**: 77/77 pasos idénticos y 16/16 de IA contra el ejecutable. 100 % cobertura (605 tests). Pendiente para T-6.6: licencia del proyecto (`UNLICENSED` en package.json).
-   [x] **T-6.3: [RELEASE] Flujo de release en GitHub Actions.**
    -   Hecho 2026-08-29: `.github/workflows/ci.yml` (push a main y PR: typecheck, lint, build, Typst en caché con `cv typst install`, cobertura 100 % con Typst real, arnés determinista `--require-typst`) y `.github/workflows/release.yml` (tags `v*` y `workflow_dispatch` de ensayo: verify + tag = versión → `package-linux-x64` con `npm run package`, arnés contra el ejecutable y artefacto `.tar.gz` + `.sha256` → `release` solo con tag: `SHA256SUMS.txt`, atestación SLSA y `gh release create` con `--prerelease` para sufijos). Permisos mínimos, sin secretos, acciones fijadas por SHA, `.node-version` 26.7.0, Dependabot. Ensayada en local la secuencia exacta (107 s, todo en verde). Listo para el primer `push`; activación y alcance restante en `docs/packaging-and-release.md` §11.
    -   Corrección 2026-08-29 (detectada en T-6.6): los workflows invocaban `npm run lint`, un script que no existe (el proyecto no tiene linter; la puerta estática es `tsc` en modo estricto): en GitHub el paso habría fallado y el ensayo local no lo ejercitó. Eliminado de ambos workflows y de la nota de diseño; ESLint queda registrado como B-6.

-   [x] **T-6.4: [RELEASE] Canales npm y Homebrew** (opcional) — pospuesto tras la v1.0.0 (2026-08-29) y **DESCARTADO por el PO el 1-sep-2026**. Las dos vías exigen cuentas y credenciales de una persona (npm con *trusted publishing*, un *tap* de Homebrew), así que no son trabajo que este proyecto pueda cerrar por su cuenta; y el canal que hoy funciona —el tar.gz verificable de cada release, más la imagen de GHCR— cubre la instalación sin pedirle a nadie que confíe en un registro más. Si algún día se quiere, la nota de diseño conserva el plan (`docs/packaging-and-release.md` §6.3).
-   [x] **T-6.5: [RELEASE] Firma y notarización** — pospuesto tras la v1.0.0 (2026-08-29) y **DESCARTADO por el PO el 1-sep-2026**. Exige un Apple Developer ID (99 $/año) y un certificado Authenticode: dinero, burocracia y cuentas de una persona, para un producto cuya plataforma de referencia es Linux. **Consecuencia asumida y escrita**: el ejecutable se publica para `linux-x64` y `linux-arm64`; en macOS y Windows el camino es la imagen de Docker (multi-arquitectura) o compilar desde el código. Lo que sí hay, y es lo que de verdad protege: `SHA256SUMS.txt`, atestación SLSA verificable con `gh attestation verify` y las imágenes con su procedencia firmada.
-   [x] **T-6.6: [RELEASE] Versión 1.0.0**: licencia, `CHANGELOG.md` y pulido final de la documentación (ordenada por el Director el 2026-08-29); el tag `v1.0.0` lo pone el Director. **APROBADA por el Director el 2026-08-29**; repositorio confirmado `nunzi00/chameleon-cv` (metadatos en `package.json`, CHANGELOG y README).
    -   Hecho 2026-08-29: **licencia MIT** (`LICENSE`; `package.json` con `license` y `author`) y **avisos de licencias de terceros** en el archivo: `scripts/package.ts` genera `THIRD-PARTY-NOTICES.md` a partir del `metafile` de esbuild (los 65 paquetes npm que de verdad viajan en los bundles, con el identificador de `package.json` y el texto de licencia del paquete instalado; 4 lo declaran sin incluir el texto), la licencia de Node.js leída de la distribución que `--build-sea` embebe (junto al ejecutable en las distribuciones oficiales, rutas de Arch y Debian, o `CHAMELEON_NODE_LICENSE`; sin ella el empaquetado se detiene) y las fuentes OFL; el archivo lleva `LICENSE`, `CHANGELOG.md`, `THIRD-PARTY-NOTICES.md` y `LICENSE-SourceSans3.md` (`src/release/notices.ts`, 100 % de cobertura). **`CHANGELOG.md`** («Keep a Changelog»; la 1.0.0 resume los Hitos 1–6) como única fuente de las notas de la release: `npm run release:notes -- <versión>` (`src/release/changelog.ts`) las extrae y exige sección con fecha y contenido; `verify` lo comprueba antes de empaquetar, `package-linux-x64` las adjunta al artefacto y `release` publica con `--notes-file`. **Pulido**: README («Qué hace», requisitos por vía de instalación, instalación desde *Releases* con `sha256sum -c` y `gh attestation verify`, opciones remotas en la tabla, «Licencia»), `docs/packaging-and-release.md` (§6, §8, estado, §11) y este ROADMAP. **Verificación**: 620 tests, 100 % de cobertura (con Typst real); arnés determinista 77/77 con `dist/` y 77/77 contra el ejecutable; `npm run package` 20,8 s (`chameleon-cv-1.0.0-linux-x64.tar.gz`); `release:notes` con la 1.0.0 y código 2 con una versión inexistente. Versión **1.0.0** en `package.json`. Pendiente del repositorio en GitHub: `repository`/`homepage` en `package.json` y los enlaces de comparación del CHANGELOG (necesitan el nombre del repositorio).

### Hito 7: Externalización y Ecosistema — «The Public Release» (ordenado por el Director de Ingeniería y Producto el 2026-08-29; **cerrado el 2026-08-30 con la v1.3.0**; queda T-7.1b, registrada por el Director como tarea futura)

Del producto núcleo a una plataforma abierta: **documentación** (la piedra angular), **containerización y despliegue** (la autonomía) e **interfaz gráfica** (la accesibilidad), en este orden. El foco pasa de implementar funcionalidades a la experiencia de desarrollador (DevEx) y de usuario (UX). La GUI será un *cliente* de la lógica central, nunca una reimplementación. El núcleo no cambia por este hito: cada tarea termina con la suite y los arneses de aceptación en verde.

**Pilar 1 — Documentación exhaustiva (Docs-as-Code)**

-   [x] **T-7.1: [DOCS] Portal de documentación**: sitio estático, completo y navegable (VitePress o Docusaurus; GitHub Pages): guía de inicio rápido (< 5 min), guía de uso por comando con cada opción y ejemplos, tutoriales guiados («Creando tu perfil desde cero», «Adaptando tu CV a 3 ofertas», «Creando tu propio tema con `cv theme create`», «Configurando el co-piloto con un modelo local (Ollama)») y sección para desarrolladores (visión arquitectónica con los cánones C1–C13, flujo parser → selector → renderer, módulos clave y diagramas; `CONTRIBUTING.md`; guías de extensión para formatos de datos y proveedores LLM).
    -   **PROPUESTA v1 en `docs/docs-portal.md` (2026-08-29), APROBADA sin reservas por el Director el 2026-08-29** (decisiones 1–7: VitePress; `website/` aislado; README puerta corta; castellano raíz; Docs-as-Code —referencia generada y tutoriales ejecutados— elevado a **estándar de toda la documentación del proyecto**; repositorio `nunzi00/chameleon-cv`; canon C14): VitePress 1.6.4 (173 paquetes frente a 472 de Starlight y 1 271 de Docusaurus; búsqueda local y enlaces muertos de serie), `website/` aislado con su propio lockfile, referencia de comandos generada desde la ayuda de la CLI, tutoriales ejecutados en CI contra el binario (C13), notas de diseño publicadas desde `docs/`, `pages.yml` con acciones fijadas por SHA, castellano como raíz e inglés en T-7.1b.
    -   Hecho 2026-08-29: `website/` (VitePress 1.6.4 fijado, lockfile propio, castellano, búsqueda local, enlaces muertos como error, repositorio leído de `package.json`): portada, guía de usuario en nueve capítulos, cuatro tutoriales ejecutables, sección de desarrolladores (arquitectura con diagramas SVG y cánones C1–C15, contribuir, pruebas, extender, empaquetado, docs-as-code, ROADMAP), notas de diseño, CHANGELOG y licencia. `scripts/docs/`: referencia generada desde commander (21 páginas + 16 ficheros de ejemplos obligatorios), sincronización con reescritura de enlaces y guarda del nombre del repositorio, ejecutor de tutoriales (25 bloques, omisiones visibles). `CONTRIBUTING.md`; README reducido a puerta corta; `pages.yml` (build en PR, despliegue en main, acciones por SHA) y Dependabot para `website/`. Verificado: `docs:check` en verde (63 páginas, 5/5 tutoriales con Typst real) y tutorial 4 íntegro con Qwen2.5-7B (25/25 bloques, 144 s). Detalle y desviaciones en `docs/docs-portal.md` §11. Activación: Pages con origen «GitHub Actions». **APROBADA sin reservas por el Director el 2026-08-29**, que canonizó el **C15, «La documentación es código verificable»** (`docs/llm-integration.md` §3) y dio la orden ejecutiva de publicar el repositorio y el portal.
-   [x] **T-7.1b: [DOCS] Portal en inglés** (segundo `locale`; registrada por el Director el 2026-08-29 como tarea futura). **Retirada el 1-sep por duplicada**: es la misma tarea que **T-9.7**, que la desarrolla y lleva su estado real —dos fases hechas y las fases 3+ pendientes—. Marcarla aquí no significa que el portal esté traducido del todo; para eso, T-9.7.

**Pilar 2 — Containerización y despliegue simplificado (Docker)**

-   [x] **T-7.2: [OPS] Ecosistema Docker**: `Dockerfile` multi-etapa (dependencias → compilación del binario → imagen final mínima con el ejecutable y sus assets) y `docker-compose.yml` con dos servicios: `chameleon-cv` (volumen local `./my-profile` para editar los datos desde la máquina anfitriona) y `ollama` (imagen oficial, preconfigurada con un modelo recomendado); documentación de `docker compose up` y de `docker compose exec chameleon-cv cv …`.
    -   **PROPUESTA v1 en `docs/docker.md` (2026-08-29), pendiente de aprobación**, con un spike medido con Docker 29.7 y Compose 5.5: imagen multi-etapa sin Node dentro (Debian por defecto con UID/GID, distroless opcional), Compose por capas (`compose.yml` sin red; `compose.ai.yml` con Ollama y `network_mode: service:ollama`, validado: loopback intacto; `compose.gpu.yml`), `run --rm` como patrón, `qwen2.5:7b-instruct`, endurecimiento por defecto, trabajo `docker` en CI y tutorial 5 (C15). Hallazgos: el ejecutable exige `libatomic` y `libstdc++` (ausentes en imágenes mínimas), `xz` en la construcción, permisos 0700 de `typst install`, ejecutable de 160 MB con el Node oficial. Siete decisiones para el Director. **APROBADA por el Director el 2026-08-29** con las siete decisiones (Debian por defecto y distroless opcional; superposiciones de Compose; `run --rm`; `qwen2.5:7b-instruct`; trabajo `docker` en la CI como no negociable; GHCR en T-7.3; `strip` registrado como B-7).
    -   Hecho 2026-08-29: `Dockerfile` (build → runtime Debian con UID/GID, y runtime-distroless; digests; etiquetas OCI; libatomic/libstdc++; Typst en la construcción), `.dockerignore`, `compose.yml` (sin red, endurecido, `./my-profile:/work`, volumen `cv-cache`), `compose.ai.yml` (Ollama por digest en loopback compartido, `ollama-pull`, modelo validado), `compose.gpu.yml`, `.env.example`, `scripts/docker-smoke.sh` (`npm run docker:smoke`, 13 comprobaciones), `npm run docker:build`, Dependabot `docker`, trabajo `docker` en `ci.yml`, guía y tutorial 5 (`needs-docker`, `files:`, `cleanup:` en el ejecutor), README y CHANGELOG. Verificado en local: imagen 104 MB comprimida (≈ 300 MB en disco), humo 13/13, Compose válido, tutorial 5 4/4 contra la imagen y omisión visible sin Docker, `docs:check` 6/6. Sin ejecutar: Ollama real en Compose (≈ 8 GB) y la CI en GitHub (sin remoto aún). Detalle en `docs/docker.md` §12. **Entrega APROBADA por el Director el 2026-08-29.**
-   [x] **T-7.3: [RELEASE] Publicación de la imagen Docker** en GitHub Container Registry desde el workflow de release, con la misma etiqueta que el binario (`ghcr.io/nunzi00/chameleon-cv:1.1.1`). Plan aprobado en esbozo (`docs/docker.md` §8, decisión 7 del 2026-08-29).
    -   **PROPUESTA v1 en `docs/ghcr-publication.md` (2026-08-29), pendiente de aprobación**: un paquete `ghcr.io/nunzi00/chameleon-cv` con etiquetas `X.Y.Z`/`X.Y`/`X`/`latest` (sin alias en prereleases) y variante `-distroless`; `linux/amd64` + `linux/arm64` en runners nativos (recomendado, con salvaguarda: arm64 solo se verifica en la CI); SBOM y procedencia de BuildKit más atestación de GitHub sobre el índice (`gh attestation verify oci://…`); trabajos `docker-build` (humo antes de empujar por digest) y `docker-publish` en `release.yml`, `workflow_dispatch` con `tag`/`push` para ensayar sin publicar y publicar retroactivamente la 1.1.0; matriz nativa en la CI; `compose.yml` con `CHAMELEON_CV_IMAGE` y prueba de coherencia de versiones; guía, README y CHANGELOG. Nueve decisiones para el Director. **APROBADA en su totalidad por el Director el 2026-08-29** (las nueve decisiones, en `REGISTRO_DE_DECISIONES.md`).
    -   Hecho 2026-08-29: `Dockerfile` multi-arquitectura, `compose.yml` con `CHAMELEON_CV_IMAGE` y prueba de coherencia de versiones (`src/release/compose.ts`), humo con `--user`, matriz nativa `amd64`/`arm64` con las dos variantes en `ci.yml`, `docker-build`/`docker-publish` y `workflow_dispatch` (`tag`, `push`) en `release.yml`, `image-scan.yml` semanal, guía «La imagen publicada», README, CHANGELOG `[1.1.1]`; verificado en local (humo 13/13 en las tres configuraciones, tutorial 5 con la variable, 701 pruebas al 100 %, arnés 78/78). Desviación de la decisión 7 (retroactiva 1.1.0 → primera imagen con `v1.1.1`, razonada en `docs/ghcr-publication.md` §10). Evidencia arm64 obtenida en la CI de GitHub el mismo día (runner nativo: construcción, humo de las dos variantes, Compose y tutorial 5). **Entrega APROBADA por el Director el 2026-08-29** («aprobación total, incondicional»); la 1.1.0 se salta oficialmente y la primera imagen es la de la **v1.1.1**, etiquetada por orden del Director. **Publicada la v1.1.1 el 2026-08-29** (run `33275465650`): imagen multi-arquitectura en `ghcr.io/nunzi00/chameleon-cv` (`1.1.1`/`1.1`/`1`/`latest` y `-distroless`), atestación de GitHub verificada con cosign y humo 13/13 contra las imágenes descargadas (`docs/ghcr-publication.md` §10). **T-7.3 cerrada.**

**Pilar 3 — Interfaz gráfica web (GUI)**

-   [x] **T-7.4: [API] Diseño de la API headless** (T-7.4a y T-7.4b entregadas y aprobadas el 2026-08-29): servidor web local (Fastify, Express o `node:http`) que envuelve las funciones principales y las expone como endpoints (`POST /api/generate`, `GET /api/profile/structure`, `POST /api/improve`…), arrancado con `cv serve`; la imagen Docker también podrá ejecutarlo.
    -   **PROPUESTA v1 en `docs/api-headless.md` (2026-08-29), APROBADA (T-7.4a y T-7.4b entregadas y aprobadas el 2026-08-29)**: capa de casos de uso `src/app/` compartida por la CLI (en proceso, sin adelgazar) y el servidor; `cv serve` sobre `node:http` sin dependencias nuevas, solo loopback, token de sesión, comprobación de `Host`/`Origin`, identificadores en lugar de rutas, remotos desactivados salvo `--allow-remote` con consentimiento en dos pasos; contrato `/api/v1` (estado, fuentes con `If-Match`, validar, compilar, perfil, generar, salida, ofertas, trabajos del co-piloto con SSE, revisiones y `apply`, temas); paridad CLI–API probada; referencia generada (C15); superposición `compose.serve.yml`; división en T-7.4a/T-7.4b; versión 1.1.0; análisis del «cliente delgado» (no recomendado). Ocho decisiones para el Director. **APROBADA en su totalidad por el Director el 2026-08-29** (las ocho decisiones, en `REGISTRO_DE_DECISIONES.md`). Ejecución en dos partes:
    -   [x] **T-7.4a**: capa de casos de uso `src/app/` (`AppContext`, `AppError`, casos deterministas) con la CLI migrada sin cambiar un byte de su salida; `src/serve/` (enrutador, registro de rutas, token, `Host`/`Origin`, errores, estático) y `cv serve`; endpoints deterministas de `/api/v1`; pruebas unitarias, de integración y escenario `serve` del arnés.
        -   Hecho 2026-08-29: `src/app/` (S1: arnés determinista 77/77 idénticos tras migrar la CLI), `src/serve/` y `cv serve` (S2–S3: 15 endpoints deterministas, token, `Host`/`Origin`, sin CORS, cuerpos acotados, errores tipificados), escenario `serve` del arnés (cliente mínimo, 25 respuestas byte a byte) en verde con `dist/` y contra el ejecutable; 666 pruebas, 100 % de cobertura; `docs:check` en verde. Detalle en `docs/api-headless.md` §12. **APROBADA por el Director el 2026-08-29** («aprobación total y sin reservas»).
    -   [x] **T-7.4b**: trabajos del co-piloto con SSE y cancelación, consentimiento en dos pasos, revisiones y `apply`; `compose.serve.yml`; guía, tutorial 6 y referencia generada de la API; CHANGELOG y versión 1.1.0.
        -   Hecho 2026-08-29: `src/app/copilot.ts` + `review.ts` + `provenance.ts` (S4: los cuatro comandos del co-piloto como clientes delgados, arnés 78/78 idénticos; `AbortSignal` de extremo a extremo), `src/serve/jobs.ts` + `consent.ts` + SSE + 12 rutas nuevas y `--allow-remote` (S5), referencia generada `/reference/api`, guía «La API local», tutorial 6, `compose.serve.yml`/`compose.serve-ai.yml`, CHANGELOG `[1.1.0]` (S6); 694 pruebas, 100 % de cobertura; arnés determinista 78/78 (35 respuestas en `serve`); `docs:check` en verde; tutorial 6 y arnés de IA ejecutados con un modelo local real. Detalle en `docs/api-headless.md` §12. **APROBADA por el Director el 2026-08-29** («aprobación total y sin reservas»); la v1.1.0 la etiqueta y publica el Director.
-   [x] **T-7.5: [GUI] MVP de la interfaz web** (entregada en dos versiones: T-7.5a → 1.2.0 y T-7.5b → 1.3.0, ambas aprobadas por el Director de Ingeniería y Producto): SPA (SvelteKit, SolidStart o Svelte/Solid + Vite) que consume la API: explorador y editor de los ficheros `.md`/`.csv` del perfil, selección de especialidades y palabras clave, «Generar CV» con visor de PDF integrado, co-piloto con comparación antes/después; servida por `cv serve` o desplegada aparte.
    -   **PROPUESTA v1 en `docs/gui-mvp.md` (2026-08-29), APROBADA en su totalidad el 2026-08-29 (diez decisiones en §10)**: SPA en `gui/` (Vite 8 + Svelte 5 + TypeScript, aislada como `website/`) que viaja en el ejecutable y en la imagen como prefijo de assets y que `cv serve` sirve en `/` por lista cerrada con CSP estricta; sesión por fragmento → `sessionStorage`; SSE sobre `fetch`; seis pantallas (Estado, Fuentes con editor CodeMirror diferido y `If-Match`, Generar con visor de PDF e informe, Co-piloto con progreso y consentimiento, Revisiones antes/después con marcas y `apply`, Salidas); lógica pura al 100 %, componentes probados y E2E con Playwright contra `cv serve` real y el ejecutable (doble local del proveedor de IA); capturas generadas. *Spike* medido: 12,6 KB gzip la aplicación, 188 KB con el editor. Dos entregas: T-7.5a (editor y generación → 1.2.0) y T-7.5b (co-piloto y revisiones → 1.3.0). Diez decisiones para el Director. **APROBADA en su totalidad por el Director el 2026-08-29** (las diez decisiones).
    -   [x] **T-7.5a** (S1 hecho 2026-08-30 y **aprobado por el Director**: contrato tipado de la API, servicio de la SPA por lista cerrada con CSP, paquete `gui/` con Estado y Fuentes verificado en Chrome contra `cv serve` real; **S2 hecho 2026-08-30**: Generar —oferta por texto, PDF o fichero, análisis de adecuación, PDF en el visor, informe de decisiones, temas— y Salidas, como *chunks* diferidos, verificadas en Chrome con Typst real; detalle en `docs/gui-mvp.md` §11). **S3 hecho 2026-08-30**: la GUI viaja en el ejecutable (`ASSET_ROOTS`, paso 3/8 de `package.ts`, humo con `cv serve`) y en la imagen (humo del contenedor con la interfaz), E2E con Playwright contra `cv serve` real y contra el ejecutable (6/6), capturas generadas, guía «La interfaz web», CHANGELOG `[1.2.0]`; CI con la E2E y release con la E2E contra el binario. **v1.2.0 publicada el 2026-08-30** (run `33279488656`; tar.gz, imagen multi-arquitectura con la interfaz dentro y atestaciones verificadas desde fuera). **T-7.5a APROBADA por el Director el 2026-08-30** (detalle en `docs/gui-mvp.md` §11); T-7.5b en curso con luz verde del mismo día.
    -   [x] **T-7.5b** (co-piloto y revisiones → 1.3.0; **entregada y aprobada sprint a sprint 2026-08-30**): **S4 hecho 2026-08-30** (pantalla Co-piloto: trabajos con progreso SSE, cancelación, consentimiento de coste; E2E con un doble local del proveedor; ruta relativa de la revisión en la API). **S5 hecho 2026-08-30** (pantalla Revisiones: antes/después con veredictos C2, marcas `[ ]`↔`[x]` guardadas con `If-Match`, plan de aplicación y escritura confirmada con copias `.bak`, borrado; E2E que aplica una propuesta a las fuentes). **S6 hecho 2026-08-30** (capturas automatizadas de Co-piloto y Revisiones, guía «La interfaz web» completa, CHANGELOG `[1.3.0]`, versión 1.3.0 en package.json/compose/guías/goldens; verificación completa antes de la orden de etiquetado). **v1.3.0 publicada el 2026-08-30** por orden del Director (run 33281572082; verificación externa registrada en `docs/gui-mvp.md` §11).

Visión posterior confirmada por el Director el 2026-08-29, **sin planificar todavía**: el Hito 7 sienta las bases (documentación, Docker, API headless) antes de abordar en hitos futuros una plataforma multiusuario y una edición visual; se especificarán cuando el Hito 7 termine.

## Hito 9 — Precisión, guardas y alcance (propuesto por el Director Técnico el 2026-08-31 a petición del PO; **hoja de ruta APROBADA en su totalidad por el PO el 2026-08-31**, `docs/roadmap-hito-9.md`)

> Cada tarea nace de algo medido o de deuda registrada, no de una lista de deseos. Reparto aprobado el 31-ago: **v1.12.0** T-9.1 + T-9.2 · **v1.13.0** T-9.3 + T-9.4 · **v1.14.0** T-9.5 + T-9.6 · sin fecha T-9.7 y T-6.5. **Lo publicado se desvió, y conviene que conste**: la v1.12.0 salió con las cuatro (T-9.1 a T-9.4, decisión del PO de agruparlas) y la **v1.13.0 con todo lo demás del hito más lo que fue surgiendo** —T-9.5 a T-9.9, el cierre de T-8.15 y las deudas B-5 a B-12—, porque el trabajo llegó junto en una sola sesión. **v1.13.1** (1-sep) llevó las correcciones del verificador C2 y `--allow-new-numbers`. **v1.14.0** (1-sep, **publicada y verificada externamente** —tag `v1.14.0` sobre `95fc158`; los siete trabajos del run `33488540100` en verde; tar.gz x64 sha256 `d07445c6…` y arm64 `7a2f6ec4…` coincidiendo con `SHA256SUMS.txt`; **una sola atestación SLSA con los dos artefactos como sujeto**, verificada con `gh attestation verify` sobre `refs/tags/v1.14.0`; `cv --version` = 1.14.0; **arnés 202/202 y E2E 13/13 contra el binario descargado**; imágenes `075abf5f…` (Debian) y `dcae9c2c…` (distroless) multi-arquitectura con su atestación verificada y las ocho etiquetas puestas—) cierra el hito con T-9.10: el co-piloto como segunda fuente de `JobRequirements`, en la CLI, en la API y en la web, más la unificación de separadores y el aviso de oferta sin requisitos. **v1.18.0** (1-sep, **publicada y verificada externamente** —tag `v1.18.0` sobre `fa5f993`; los siete trabajos del run `33561336225` en verde, más CI y Docs; tar.gz x64 sha256 `a517964b…` y arm64 `7f970e8d…` coincidiendo con `SHA256SUMS.txt`; **una sola atestación SLSA con los dos artefactos como sujeto**, verificada con `gh attestation verify` sobre `refs/tags/v1.18.0`; `cv --version` = 1.18.0; **arnés 202/202 y E2E 13/13 contra el binario descargado**; imágenes `9b42e41a…` (Debian, también `1.18` y `latest`) y `13a48a04…` (distroless) con su atestación verificada—) cierra el bucle de las etiquetas (`--apply`, T-9.15), lleva a la web comparar ofertas e importar una carpeta (T-9.13 y T-9.14 por API), dice qué revisiones ya se aplicaron (T-9.17), espera y reintenta tras un 429 (T-9.16), y recuerda cómo generas (T-9.18). **v1.17.0** (1-sep, **publicada y verificada externamente** —tag `v1.17.0` sobre `88c4889`; los siete trabajos del run `33535559859` en verde; tar.gz x64 sha256 `97baf64a…` y arm64 `b44fd810…` coincidiendo con `SHA256SUMS.txt`; atestación SLSA verificada sobre los dos; `cv --version` = 1.17.0; **arnés 202/202 y E2E 13/13 contra el binario descargado**, y con ÉL las dos órdenes nuevas sobre los datos reales del PO —tres ofertas comparadas y seis CV importados—; imágenes `3ad2b1e0…` (Debian) y `6f1d5fcb…` (distroless) con su atestación verificada—) añade comparar varias ofertas (`--rank`, T-9.13) e importar una carpeta entera (`--all`, T-9.14). **v1.16.0** (1-sep, **publicada y verificada externamente** —tag `v1.16.0` sobre `8ee2bbb`; los siete trabajos del run `33529366414` en verde; tar.gz x64 sha256 `ec0a9251…` y arm64 `4ebdb042…` coincidiendo con `SHA256SUMS.txt`; atestación SLSA verificada sobre los dos artefactos; `cv --version` = 1.16.0; **arnés 202/202 y E2E 13/13 contra el binario descargado**; imágenes `15608730…` (Debian) y `519f87e4…` (distroless) con su atestación verificada—) recoge B-14 (el nombre espaciado), el arnés en paralelo, el registro de rutas por dominios con el consentimiento en un solo sitio, y T-9.12 (cerrar el bucle del alias, eligiendo una a una). **v1.15.0** (1-sep, **publicada y verificada externamente** —tag `v1.15.0` sobre `b863ef3`; los siete trabajos del run `33513588421` en verde; tar.gz x64 sha256 `7e3a68c9…` y arm64 `11fee959…` coincidiendo con `SHA256SUMS.txt`; atestación SLSA con los dos artefactos como sujeto, verificada sobre `refs/tags/v1.15.0`; `cv --version` = 1.15.0; **arnés 202/202 y E2E 13/13 contra el binario descargado**; imágenes `1c67dc84…` (Debian) y `4a524864…` (distroless) multi-arquitectura con su atestación verificada y las ocho etiquetas puestas—) recoge lo que salió después de cerrar el hito: B-13 (el PDF de LinkedIn), T-9.11 (elegir qué NO entra), el portal entero en inglés con sus páginas ejecutándose en CI (T-9.7), y los dos defectos que el PO encontró usando T-9.10 —el esquema que Groq rechazaba y la adecuación cayendo debajo del formulario—.

-   [x] **T-9.1: [IMPORT] El nombre y la cabecera, bien reconocidos** (evidencia: de once PDF del corpus, **uno** reconocía bien el nombre; los demás tomaban un título de página —«Chronological», «EXAMPLE RESUME», «SAMPLE RESUMES»— y ese título bautizaba además la carpeta del borrador). **HECHA el 31-ago**: `nameScore` puntúa los candidatos de la cabecera (2–5 palabras capitalizadas, partículas en minúscula, sin cifras ni palabras de documento o institución), `chooseName` los busca en las seis primeras líneas partiendo por separadores y por el pegado nombre+titular, y sin candidato no se inventa nada («Nombre pendiente» con aviso; la carpeta pasa a llamarse por el fichero). Resultado en el corpus: aciertan los seis CV con persona y las cinco guías quedan pendientes.
-   [x] **T-9.2: [QA] Guarda de clases de estilo en la interfaz** (evidencia: `cv-pre` se publicó en la 1.11.0 sin existir en `app.css`; no lo vieron ni `tsc`, ni `svelte-check`, ni 176 pruebas). **HECHA el 31-ago** (`gui/src/styles.test.ts`): toda clase `cv-*` aplicada debe existir en la hoja global o en el `<style>` del componente; en su primera ejecución encontró otras tres (`cv-field-row`, `cv-warning`, `cv-provider-warning`), ya corregidas.
-   [x] **T-9.3: [QA] ESLint como segunda puerta estática** (deuda B-6). **HECHA el 31-ago**: `eslint.config.mjs` con `typescript-eslint` y doce reglas de señal (promesas sin esperar o mal usadas, `await` sobre no-promesas, aserciones inútiles, `sort` sin comparador, métodos desligados), `npm run lint` y el paso en CI y en el trabajo de verificación de la release; alcance `src/**` y `tests/**` (los `scripts/**` no están en ningún tsconfig, ni siquiera para `typecheck`: queda anotado). Primera pasada: 21 hallazgos reales → 11 aserciones retiradas, 2 «arreglos» revertidos por incorrectos (el tipo de `process.stdin.isTTY` miente en una tubería; `?: T | undefined` es deliberado con `exactOptionalPropertyTypes`) y la regla de rechazos sin `Error` apagada solo en pruebas, que rechazan con valores raros a propósito.
-   [x] **T-9.4: [RELEASE] `strip` del ejecutable SEA** (deuda B-7). **MEDIDA y CERRADA COMO «NO PROCEDE» el 31-ago**: sobre el binario real de la 1.11.0, `strip` avisa de que la sección `.note.node.sea` no está en ningún segmento, ahorra **0,3 MB de 74** (0,4 %) y **rompe el ejecutable** (SIGSEGV, código 139); las variantes seguras (`--strip-debug`, `--strip-unneeded`, incluso con `--keep-section=.note.node.sea`) lo rompen igual. El peso no son símbolos: `.text` 34,4 MB y `.rodata` 20,2 MB son el propio Node (ya *stripped*) y `.note.node.sea` 14,5 MB es nuestro paquete. Adelgazar exigiría otra vía (comprimir el blob o un Node a medida), que ya no es B-7.
-   [x] **T-9.5: [IMPORT/GUI] Aplicar una propuesta del co-piloto** al borrador con confirmación explícita (**D3 aprobada por el PO**: el botón mueve la línea; el modelo sigue sin aplicar nada). **HECHA el 31-ago** (`docs/cv-import.md` §7): el esquema decide qué puede aplicarse con la línea sola —habilidad, logro, resumen, proyecto, certificación y descartar— y qué exige un formulario mínimo —idioma (nivel MCER, precargado si la línea lo declara), contacto (qué campo), experiencia (empresa, puesto, inicio) y formación (institución, titulación)—; **decisión del PO: formulario en la GUI en vez de marcadores «COMPLETAR»**, para que el borrador nunca deje de validar y no se invente un dato. Un solo camino en el núcleo (`src/app/import-apply.ts`): releer con `loadSources`, añadir la entidad, replanificar con `planFiles` y escribir solo lo que cambia; si la entidad no cumpliera el esquema no se escribe nada. Ruta `POST /api/v1/import/apply` (síncrona, sin modelo) y botón por propuesta con diálogo de confirmación en `Perfil → Importar CV`; la línea sale de «Sin situar» y queda en «Aplicado» con su destino.
-   [x] **T-9.6: [RELEASE] `linux-arm64` en la release** (el tar.gz arm64 no exige firma; macOS y Windows quedaron fuera al descartarse T-6.5 el 1-sep: allí el camino es Docker o compilar). **HECHA el 31-ago**: el trabajo `package-linux-x64` pasa a ser `package` en matriz (x64 en `ubuntu-24.04`, arm64 en `ubuntu-24.04-arm`), con `fail-fast: false` para que un fallo de una arquitectura no se lleve la otra por delante. El nombre del archivo ya salía de `process.arch` y el trabajo `release` ya descargaba con `pattern: chameleon-cv-*` y `merge-multiple`, así que `SHA256SUMS.txt`, la atestación SLSA y `gh release create` cubren las dos sin tocarlos. Dos decisiones: las notas del CHANGELOG viajan solo con el artefacto x64 (son las mismas y `merge-multiple` las fundiría) y el E2E de Playwright se queda en x64 (misma interfaz en ambas; el arnés determinista sí corre contra el ejecutable arm64). Pendiente de comprobar en la primera release que lo use.
-   [x] **T-8.15 (cierre): [LLM] Gemini verificado y activado** (encargo del PO del 31-ago: «quiero gestionar Gemini sigue como pending-verification»). Protocolo de `docs/copilot-providers.md` §9 ejecutado con la clave del PO y **solo con el banco de pruebas**; registro completo en §11. **Pasó tras corregir tres defectos que destapó**: el prefijo `models/` de los identificadores hacía que dijéramos «modelo no disponible» con el modelo primero de la lista; la capa compatible **rechaza `seed`** (HTTP 400) y tumbaba todas las llamadas; y `gemini-2.5-flash` responde **HTTP 404 «no longer available to new users»** aunque el endpoint de modelos lo siga listando, con su sustituto truncando el JSON por el techo de tokens. Resultado final: 6 propuestas, 3 aceptadas y 3 rechazadas por código (C2), y el 429 del plan gratuito cortó el lote sin reintentar. **Criterio (b) de §0 confirmado por el PO el 31-ago: el alta en el plan gratuito NO exigió método de pago.** Verificación cerrada del todo.
-   [x] **T-9.10: [LLM/OFERTAS] El co-piloto como segunda fuente de `JobRequirements`** (encargo del PO del 1-sep-2026: «cuando se facilita una oferta para generar el cv se puede meter un llm para decidir?»). **Forma acordada con el PO: el modelo LEE la oferta, no decide el CV.** Es el gancho que `docs/scoring.md` §8 dejó anotado desde el diseño de T-2.4 y que ahora se desarrolla en su §11: `extractJobRequirements` gana una implementación alternativa por modelo que devuelve el **mismo** `JobRequirements`, validado con `JobRequirementsSchema`; el *scoring*, la selección y el informe no cambian.
    -   **Por qué no decide**: `--explain` dejaría de poder decir por qué entró cada ítem (hoy: `universal`, `match`, `via-achievements`, `pinned`); se perdería la reproducibilidad que el arnés comprueba con 202 pasos byte a byte; y contradice C2.
    -   **El hueco que sí llena**: el matcher es literal. Si la oferta pide «arquitectura orientada a eventos» y tus skills dicen «Kafka», no casa sin alias. El modelo tiende ese puente semántico.
    -   **Mecánica**, la misma de `suggest tags` e `import map`: se le envía la oferta y el **vocabulario cerrado** del perfil; devuelve **solo tags que ya existen**; el código verifica cada una y rechaza lo que no esté; opt-in con `--copilot`, con consentimiento de coste y egreso explícito; `--explain` distingue qué vino del modelo y qué del matcher.
    -   **MEDIDO el 1-sep-2026 sobre tres ofertas reales del PO** (Holded, Cyberhaven y una de plataforma en GCP). De **12 términos distintos marcados como carencia, solo 3 eran falsos**: `ci/cd` (puntuación: la oferta usa barra y el perfil guion), `testing` (el perfil lo dice con `calidad`, Vitest, pytest, testcontainers) y `jwt` (en la prosa, sin skill). El resto —javascript, security, performance, java, kotlin, go, terraform— son carencias **reales**: ningún modelo las inventa. Y una de las tres ofertas **no contiene requisitos técnicos**: su stack está tras un enlace, así que un LLM leería la misma nada.
    -   **Los tres huecos falsos se cerraron sin modelo** (mismo día): unificando los separadores en el normalizador y añadiendo dos skills al perfil del PO. Resultado medido: 28 → 30 y 4 → 2 carencias en una oferta; 4 → 5 y 8 → 7 en otra.
    -   **Recomendación del Director Técnico: no construirlo todavía** —con este corpus el modelo aportaría un término, y hasta ese salía más barato como alias—. **El PO decide construirlo igualmente el 1-sep-2026**; queda registrada la medida para que la decisión sea informada y para poder volver a medirla cuando haya más ofertas.
    -   **HECHA el 1-sep-2026** (`docs/scoring.md` §11.5): tarea `offer map` (`prompts/offer-map.v1.md`, `src/llm/tasks/offer-map.ts`) con **vocabulario cerrado por `enum` en el esquema** que se envía al proveedor, y **dos guardas verificadas por código**: la etiqueta ha de estar en el vocabulario enviado y la evidencia ha de aparecer **literalmente** en la oferta (comparada con el mismo normalizador del emparejado). Se cuentan por motivo las descartadas —`unknownTag`, `unverifiedEvidence`, `alreadyKnown`, `duplicate`— para que el informe lo explique. Casos de uso en `src/app/offer-map.ts` (`profileTags`, `planOfferMap`, `offerMapEstimate`, `mergeOfferMap`, `executeOfferMap`), `analyzeOffer` con `copilot` opcional, y `cv analyze-offer --copilot [--provider/--model]` con consentimiento de coste antes de enviar.
    -   **Tres decisiones que sostienen «el modelo no manda»**: (1) lo único del candidato que sale es la **lista de etiquetas**, sin nombres de skill ni nada del perfil (C4); (2) una etiqueta añadida por el co-piloto pesa como **una evidencia única, sin refuerzo por frecuencia**, así que nunca supera a un término que la oferta nombra tres veces, y `Math.max` impide que rebaje lo que el emparejado literal ya puntuó más alto; (3) `--explain` marca el origen (`, co-piloto`), porque sin eso no se sabría qué parte de la adecuación descansa en un modelo.
    -   **Verificado en vivo con Ollama sobre una oferta real del PO**: 5 propuestas, **3 verificadas y 2 descartadas por el código**. Y el hallazgo que justifica enseñar siempre la evidencia: una de las verificadas (`kafka`) era un disparate —la frase existía en la oferta, pero no sostenía la etiqueta—. **El código puede comprobar que la evidencia está; que la sostenga solo puede juzgarlo la persona**, y por eso el informe la imprime entera en vez de resumir «3 etiquetas añadidas».
    -   **Web y API el 1-sep** (C14: una sola vía para los tres clientes): `POST /analyze-offer` acepta `copilot` —403 `remote-disabled`, 409 `consent-required` con `estimateId` de un solo uso, como `offers/fetch`, porque es UNA petición y no un lote— y en `Generar` hay la casilla «Refinar la lectura con el co-piloto» con su selector de proveedor, el diálogo de coste y la aportación dentro del panel de adecuación, cada etiqueta con su evidencia. El término lleva su origen también en la web (`desirable · 0.75 · co-piloto`). E2E nueva contra el doble del proveedor: 13/13.
    -   Cobertura 100 % (1275 pruebas en la raíz, 190 en la GUI), arnés determinista 14 escenarios · 202 pasos, E2E 13/13, lint, typecheck y portal en verde. Guía del portal («Refinar la lectura de la oferta con el co-piloto») con el consejo que la medida dejó claro: si el puente que tendió el modelo te sirve, **el arreglo barato y permanente es el alias en `skills.csv`**.
-   [x] **T-9.18: [GUI] Que la interfaz recuerde cómo generas** (propuesta del Director Técnico del 1-sep al revisar la UI, aprobada por el PO: «continúa hasta el final»). La pantalla de Generar tiene una docena de decisiones y casi siempre son las mismas; volver a elegirlas en cada visita era trabajo que la interfaz podía ahorrar. **HECHA**: `src/lib/generate/remember.ts` guarda **solo decisiones de forma** —especialidad, formato, motor, tema, idioma, límites, compacto— y las restaura comprobando que sigan existiendo. **Lo que no se recuerda es la parte importante**: la oferta, la selección de skills y proyectos, `build` y el co-piloto con su proveedor; dejar marcado algo que envía datos sería decidir por la persona, y eso es justo lo que el producto no hace (C2, C11). Se guarda al generar, no al teclear. Probado de punta a punta en el navegador real (recargar y ver que vuelve lo que debe y no vuelve lo que no).
-   [x] **T-9.17: [REVISIONES] Saber qué revisiones ya se aplicaron** (encargo del PO del 1-sep: «cuando se aplican revisiones es necesario distinguir cuáles ya han sido aplicadas»). Aplicar dos veces ya dejó de ser un error, pero para enterarse había que intentarlo. **HECHA**: `reviewStatus` compara cada ítem con lo que hay **hoy** en las fuentes y devuelve `applied`, `pending`, `changed` o `unknown`, y `reviewProgress` lo resume para la lista. **Se mira el texto, no una marca guardada**: una revisión aplicada y luego deshecha con `cv history restore` vuelve a salir como pendiente, que es lo cierto —una marca en el fichero mentiría—. Se lee cada fuente una sola vez por listado. En la web, la etiqueta en cada ítem y el recuento junto al nombre; en la API, `progress` en `GET /reviews` y `statuses` en `GET /reviews/{name}`.
-   [x] **T-9.15: [LLM/CLI/API/GUI] Que las etiquetas sugeridas se puedan aplicar** (encargo del PO del 1-sep: «cierra la de suggest tags»). `cv suggest tags` proponía y te dejaba la línea para copiar y pegar: la mitad del trabajo, y la mitad que el producto podía hacer sin riesgo. **HECHA**: `src/app/tags-apply.ts` decide (`planApplyTags`) y escribe (`applyTags`) por separado, para poder enseñar el plan antes de tocar el disco, y con las mismas garantías que `cv improve apply` —**solo las etiquetas que la viñeta no tenía**, al final de su línea y detrás de las que ya llevara; **cambio mínimo**, el resto del fichero byte a byte igual; **copia `.bak` previa** que nunca se sobrescribe; y **comprobación de que el logro sigue estando tal cual**, porque entre sugerir y aplicar pudo editarse a mano—. Un fichero se lee, se modifica entero en memoria y se escribe una sola vez: una pasada lo deja coherente o no lo deja tocado. **Eliges una a una** (C2, C9): en la terminal se pregunta logro a logro, con `--yes` —o sin terminal— entran todas, que es lo que espera un script; en la web, cada logro enseña sus etiquetas nuevas con su casilla —**ninguna marcada**— y «Aplicar en mis fuentes» escribe solo las marcadas. Una vía para los tres clientes (C14): `POST /api/v1/tags/apply` con el plan completo en la respuesta —lo escrito y lo que no, con su motivo—. Un texto suelto no tiene dónde aplicarse y se dice, en vez de escribir a ciegas.
-   [x] **T-9.16: [LLM] Esperar y reintentar cuando el proveedor dice cuánto** (encargo del PO del 1-sep: «me gustaría que esperara el tiempo que indican y reintentar; también la opción de cancelar reintentos»). Hasta ahora un 429 detenía la tanda a la primera (C11: sin reintentos), y con un plan gratuito eso significa perder media tanda por quince segundos. **HECHA**: `src/llm/quota-retry.ts` con `retryOnQuota` y una espera cancelable, usada por las tres tareas de lote. **Tres límites, porque esperar sin freno es peor que no esperar**: solo se espera si el proveedor **dice cuánto** (sin `retry-after` no se inventa nada), nunca más de **120 s** —una cuota diaria se para y se explica— y como mucho **dos veces**. La espera escucha la misma `AbortSignal` que corta el lote, así que el botón «Cancelar» de la web y `Ctrl-C` la interrumpen; `--no-wait-quota` la desactiva en las tres órdenes. **Un fallo que el encargo destapó**: el `retry-after` **se perdía al interpretar la respuesta** en las cinco tareas —el error conservaba código y mensaje pero no los segundos—, así que ni siquiera se podía enseñar bien; ahora viaja entero y hay una prueba que lo comprueba en las cinco a la vez.
-   [x] **T-9.13: [OFERTAS] Comparar varias ofertas de una vez** (propuesta del Director Técnico del 1-sep tras ver que el PO las analizaba una a una; encargada por el PO el mismo día). **HECHA**: `--rank` sobre `cv analyze-offer`, con `src/app/rank.ts` como agregación —el motor es el `analyzeOffer` de siempre, ejecutado una vez por oferta— para que una futura pantalla web lo reutilice (C14). **Sin métricas nuevas**: cada columna es lo que ya verías analizando esa oferta sola. El orden es por **imprescindibles cubiertos** y luego por adecuación, porque lo que cierra puertas es un imprescindible sin cubrir. El perfil se compila y se comprueba **una vez**, antes de mirar ninguna oferta: si falla, falla la comparación entera y se dice una sola vez; a partir de ahí, lo que salga mal es de la oferta que lo provoca y no tumba a las demás. `--rank` no se combina todavía con `--copilot`, que se pide oferta a oferta con su coste.
    -   **Web y API el 1-sep** (C14): `POST /api/v1/offers/rank` y, en `Generar → «Del espacio»`, «Comparar varias ofertas» con la misma tabla y un botón «Usar esta» que deja la elegida en el formulario. Sin red y sin modelo: no hay coste que confirmar.
-   [x] **T-9.14: [IMPORT] Importar una carpeta entera** (misma propuesta y mismo encargo). **HECHA**: `cv import-cv <carpeta> --all` con su tabla comparativa. **Un detalle que solo aparece al probarlo con el corpus real**: los seis CV eran de la misma persona y todos querían la carpeta `import/lucas-nunzi-lopez`, así que solo entraba el primero; el borrador toma ahora el nombre del **fichero**, que además deja ver de dónde salió cada uno. Un CV que falle se anota y no detiene a los demás, que es lo que hace útil mirar un corpus de una vez —y es justo lo que caza regresiones como B-13 o B-14—.
    -   **Web y API el 1-sep** (C14): el bucle se movió al núcleo (`importCvFolder`), y de ahí salen `POST /api/v1/import-cv/folder` y la tercera opción del selector «Origen» en `Importar CV`, con su tabla comparativa y el botón de reimportar sustituyendo.
-   [x] **T-9.12: [OFERTAS/LLM] Cerrar el bucle del alias** (propuesta del Director Técnico del 1-sep, aprobada por el PO: «adelante con tu orden»). La guía ya decía que si el co-piloto tuvo que tender un puente semántico lo barato era añadir el alias a `skills.csv`; hacerlo era copiar y pegar. **HECHA el 1-sep**: `cv analyze-offer --copilot --save-aliases` lo escribe, y a partir de ahí esa oferta y las que hablen igual **se resuelven sin red y sin modelo**. `src/app/aliases.ts` decide (`planAliases`) y escribe (`saveAliases`) por separado, para poder enseñar el plan antes de tocar el disco. **Dos guardas**: solo lo que el código verificó, y solo cuando la etiqueta pertenece a **una sola** skill —si son varias, el alias no es de ninguna en particular y se dice de cuáles, para que elija la persona—; lo que ya se reconocía tampoco se duplica. **Escritura quirúrgica**, como la de `cv improve apply`: se añade al final de la columna `aliases` de esa fila y el resto del fichero queda byte a byte igual, comillas incluidas. **Un fallo que solo apareció probándolo en vivo** (y que justifica la demostración): la frase se escribía tal cual y el esquema de `skills.csv` **solo admite minúsculas sin diacríticos**, así que «orquestación de contenedores» dejaba unas fuentes que `cv build` rechaza —peor que no guardar nada—. Ahora se guarda normalizada, que además es como el emparejado la busca, y lo que no quepa (una coma, más de 60) se dice y no se escribe. **Demostrado de punta a punta**: la misma oferta pasa de 3 términos y `kubernetes` sin reconocer a **4 términos y adecuación 4/4, sin modelo y sin red**, con `cv build` en verde. **Web y elección uno a uno HECHAS el 1-sep** (encargo del PO: «cuando me sugiere etiquetas debe decirme cuáles y permitirme añadir manualmente las que yo decida de esa lista»): `POST /api/v1/aliases` con el plan completo en la respuesta, y en `Generar` cada aportación del co-piloto con su casilla —**ninguna marcada**— y el botón «Guardar N como alias». En la terminal se pregunta **una a una**; con `--yes` o sin terminal entran todas, que es lo que espera un script. C2 llevado hasta el final: el modelo propone, y quien elige es la persona, ítem a ítem.
-   [x] **T-9.11: [CLI/API/GUI] Elegir qué NO entra** (encargo del PO del 1-sep: «al generar un cv además de seleccionar qué proyectos y skills me interesa añadir, me interesa lo mismo pero para excluir»). **HECHA el 1-sep**: `--exclude-skills` y `--exclude-projects` son la otra mitad de `--skills`/`--projects` y comparten todo con ellas —emparejado por id o por nombre, aviso de lo que se nombra y no existe, y el recorte contado en el informe—. El núcleo pasa a tener `partitionByList` y de ahí salen `keepListed` y `dropListed`, que es la misma pregunta leída al derecho y al revés; primero se aplica la inclusión y sobre su resultado la exclusión, así que las dos listas se combinan. En la web, cada selector de «Afinar el contenido» gana un conmutador «Solo estas» / «Todas menos estas» —una sola lista y un modo, en vez de dos selectores—, y en la API son `excludeSkills` y `excludeProjects`. **Alcance deliberado**: quitar una skill la saca de la sección de habilidades y **no** reescribe las «Tecnologías» que un empleo declara, porque eso es un hecho de ese empleo (mismo criterio que `--skills`).
-   [x] **T-9.9: [GUI/LLM] Guardar la clave de un remoto desde Ajustes** (encargo del PO del 31-ago: «me gustaría que en ajustes web pudiera setear la clave»). Hasta hoy la propia pantalla decía lo contrario —«las claves… nunca pasan por esta página»— y era una decisión deliberada de T-8.2, así que se revisó antes de cambiarla: el servidor solo escucha en loopback, exige el token de sesión y **no registra los cuerpos** de las peticiones, y `GET /config/llm` ya daba únicamente la procedencia. **HECHA el 31-ago**: `PUT` y `DELETE /api/v1/config/llm/keys/{proveedor}` sobre `writeApiKey`/`removeApiKey` (el mismo fichero 0600 que la CLI) y campo por proveedor en Ajustes. **Lo que se conserva de la promesa, que es la mitad que importaba: las claves no se LEEN nunca**; la clave viaja una sola vez en el cuerpo, ninguna respuesta la devuelve ni enmascarada, el campo se vacía al guardar y si una variable de entorno la tapa, la página lo dice. Documentado el cambio de criterio en `docs/llm-integration.md` y en la guía del portal.
-   [x] **T-9.8: [IMPORT] Importar desde LinkedIn** (encargo del PO del 31-ago: «a partir de la url de linkedin se descargue el cv y lo analice y lo use como fuente»). **La URL se descartó con medida y el PO eligió la exportación oficial**: el `robots.txt` de LinkedIn dice `User-agent: *` → `Disallow: /` con aviso expreso, y una URL de perfil devuelve el muro de acceso («Sign in» ×21, «Join now» ×9), no el CV. **HECHA el 31-ago** (`docs/cv-import.md` §8): `src/import/linkedin.ts` lee los nueve CSV que importan del zip con el lector de zip contenido y produce el MISMO `DraftProfile` que el importador de PDF; `cv import-linkedin <zip>`; y la escritura del borrador se extrajo de `import-cv.ts` a `writeDraft`, compartida por todos los orígenes (C14), en vez de duplicarla. Sin red. Ventaja medida: al venir de datos estructurados **no queda ninguna línea «sin situar»**, frente a las 3–109 que deja el mismo perfil importado como PDF.
    -   **Pantalla web HECHA el 31-ago**: selector «Origen» en `Perfil → Importar CV` y `POST /api/v1/import-linkedin` (cuerpo binario zip) sobre el mismo núcleo. **DECIDIDO el 1-sep por el PO: se descarta admitir la exportación «completa»**. Trae decenas de CSV de mensajes, anuncios y conexiones que no son un CV, tarda hasta 24 horas en generarse frente a los diez minutos de la selección concreta, y no aporta ni un dato que la selección no traiga: los nueve CSV que se leen están en las dos. Admitirla solo añadiría superficie —ficheros que ignorar y datos personales que no queremos ni tocar— a cambio de nada. La guía ya recomienda la selección concreta y así se queda.
-   [x] **T-9.7: [DOCS] Portal en inglés** (T-7.1b, registrada por el Director el 2026-08-29). **PRIMERA FASE HECHA el 31-ago**: `website/.vitepress/config.mts` reorganizado en dos `locales` (castellano en la raíz, inglés en `/en/`) con nav, barra lateral, buscador y textos de interfaz propios; traducidas la portada (`src/en/index.md`) y el inicio rápido (`src/en/guide/quickstart.md`). Portal construyendo con las dos y los siete tutoriales en verde.
    -   **Decisión**: lo no traducido se enlaza **marcado «(es)»** desde una sección «Not translated yet» de la barra lateral inglesa, en vez de ocultarlo o de dejar enlaces que parecen ingleses y no lo son.
    -   **DECISIÓN DEL PO (31-ago), la que desbloqueaba el resto: la CLI se queda en castellano** y el portal inglés documenta una herramienta que habla castellano. Descartadas las otras dos salidas —traducir la CLI (exige i18n en el núcleo, cientos de cadenas y **duplicar o parametrizar los 202 goldens** del arnés, que es la red que caza casi todo) y pasarla a inglés dejando el portal castellano (le da la vuelta al proyecto)—. Consecuencia directa: la **referencia de comandos, que se genera de `--help`, se queda en castellano** y se enlaza marcada «(es)» mientras eso no cambie.
    -   **FASE 2 HECHA el 31-ago**: traducidas las cuatro páginas del camino esencial —`concepts`, `sources`, `generate` y `security`—, que es lo que alguien de fuera necesita para entender el producto y decidir probarlo.
    -   **FASES 3 y 4 HECHAS el 1-sep, tarea CERRADA**: traducidas las diez guías que quedaban (`import`, `offers`, `copilot`, `copilot-settings`, `web`, `api`, `docker`, `typst-themes`, `portability`, `troubleshooting`) y los **seis tutoriales** con su índice. La barra lateral inglesa pierde la sección «Not translated yet»: lo que queda en castellano ya no es deuda de traducción sino contenido **generado** —la referencia, que sale de `--help`, y la galería, que sale de los `theme.toml`— más las notas de diseño, y la sección se llama ahora «Generated or internal (Spanish)».
    -   **Decisión de la duda que quedaba abierta —si se duplicaba la ejecución en CI— resuelta a favor de ejecutarlas**: los bloques de órdenes de las páginas inglesas son **idénticos** a los castellanos a propósito (solo se traduce la prosa), así que ejecutarlos no es redundante: es lo único que garantiza que una traducción no se aleje de las órdenes que documenta. `scripts/docs/tutorials.ts` recorre ahora los dos idiomas y el inicio rápido inglés pasa a tener bloques ejecutables y su `verify`. De **7 tutoriales verificados a 14**, y el coste es 13 s más (13 → 26 s).
    -   **Lo anterior, para el registro (fases 3+)**: `import`, `offers`, `copilot`, `web`, `api`, `docker`, `typst-themes`, `copilot-settings`, `portability` y `troubleshooting`; los seis tutoriales. `theme-gallery` (585 líneas) **se genera** de los `theme.toml` con `scripts/docs/themes.ts`: traducirla a mano no procede. Y `scripts/docs/tutorials.ts` ejecuta una lista fija (`guide/quickstart.md` y `tutorials/`), así que las páginas inglesas **no se verifican en CI**: al traducir cada tutorial habrá que decidir si se duplica la ejecución —doblando ese tiempo— o basta con la castellana.

## Hito 8 — Ecosistema abierto y co-piloto avanzado (plan post-Hito 7, propuesto por el Director el 2026-08-30)

> Releases del hito: **v1.7.0** (2026-08-30: temas T-8.7, selección explícita, historial de ofertas, modelos por acción) y **v1.8.0** (2026-08-30, publicada y verificada externamente —tar.gz sha256 `9e461c01…`, imágenes `f4de4e3b…`/`213ead68…` multi-arch con cosign, arnés 186/186 contra el binario descargado—: rediseño de la GUI S1–S3, T-8.8, T-8.9, T-8.10, qwen3:8b por defecto (T-8.11), 27 temas (T-8.12) y catálogo de modelos locales (T-8.13)). **v1.8.1** (2026-08-30, publicada y verificada externamente —tar.gz sha256 `dbb2d82b…`, imágenes `bc345422…`/`5139615…` multi-arch con cosign, arnés 189/189 contra el binario descargado—: runtime de Ollama adaptativo T-8.14 y portal del rediseño T-8.6 S4). **v1.9.0** (2026-08-31, publicada y verificada externamente —tar.gz sha256 `b8e0c4fc…`, imágenes `4c93feda…`/`dcea788a…` multi-arch con cosign y atestación SLSA, arnés 202/202 contra el binario descargado, humo de ambas imágenes en verde—: ingesta de ofertas desde URL T-8.5 al completo —extractor con procedencia, API con consentimiento en dos pasos, GUI—, importador de CV T-8.4b F1 con pantalla web, corte de cuota en lotes y suelo de tokens por modelo). **v1.10.0** (2026-08-31, publicada y verificada externamente —tar.gz sha256 `2c820cd0…`, imágenes `9cf81c29…`/`ca820a60…` multi-arch con cosign y atestación SLSA, arnés 202/202 contra el binario descargado, humo de ambas imágenes en verde—: 37 temas T-8.16, conmutador de remotos persistido T-8.17, proveedor Gemini pendiente de verificación T-8.15 e importación de CV con co-piloto y avisos de calidad T-8.4b F2). **v1.11.0** (2026-08-31, publicada y verificada externamente —tar.gz sha256 `66717ef7…`, imágenes `a875df9e…`/`729636cb…` multi-arch con cosign y atestación SLSA, arnés 202/202 contra el binario descargado, humo de ambas imágenes en verde—: botón «Refinar con el co-piloto» en la pantalla web de importación, T-8.18). Siguiente: activación de T-8.15 cuando un humano verifique el alta de Gemini.

Se inicia al cerrar el Hito 7; cada tarea tendrá su especificación técnica (PROPUESTA → aprobación) antes de implementarse, como hasta ahora. Observaciones del Director Técnico registradas al validar el plan:

-   [x] **T-8.1: [DATOS] Portabilidad fundacional** (**entregada 2026-08-30**, aprobada sprint a sprint; **v1.4.0 publicada el 2026-08-30**, run 33284273921, verificación externa en `docs/portability.md` §11): `cv export` del perfil canónico (`profile.json`, ya existe como artefacto) y `cv import` desde ese formato estructurado. *Nota*: la fuente de verdad son las fuentes Markdown/CSV (C9); importar un `profile.json` exige regenerar fuentes a partir del perfil (la transformación inversa a `cv build`), no copiar el artefacto: es la parte con diseño real.
    -   **PROPUESTA v1 en `docs/portability.md` (2026-08-30), APROBADA en su totalidad por el Director el 2026-08-30 (10/10)**; **S1 hecho 2026-08-30** (serializadores Markdown/CSV, árbol en memoria, `src/app/portability.ts` con auto-chequeo; ida y vuelta verificada sobre los datasets reales). **S2 hecho 2026-08-30** (`cv export`/`cv import`, `GET /export`/`POST /import`, escenario `portability` del arnés con la ida y vuelta en vivo, referencia regenerada, C9 clarificado). **S3 hecho 2026-08-30** (guía, §15 del formato, GUI mínima en Estado, README, CHANGELOG `[1.4.0]`, versión 1.4.0). Diseño: el formato de intercambio es el propio `MasterProfile` JSON; `cv export` desde las fuentes; `cv import` regenera las fuentes canónicas con auto-chequeo (re-análisis y comparación antes de escribir), solo sobre destino vacío o con `--replace` y copia completa; API `GET /export`/`POST /import`; GUI mínima opcional; 1.4.0.
-   [x] **T-8.2: [IA] Configuración avanzada del co-piloto** (**implementación entregada 2026-08-30**, aprobada sprint a sprint; v1.5.0 preparada y, por decisión del Director del 2026-08-30, publicada **sin proveedor gratuito seleccionable**: Groq queda en el registro como `pending-verification` hasta que una persona complete el protocolo de `docs/copilot-providers.md` §9): modelo y proveedor local en `cv.toml`, expuestos en una pantalla «Ajustes» de la GUI; uno o dos proveedores externos con plan gratuito, con claves gestionadas según C11 (consentimiento, sin telemetría, cuotas visibles). *Nota*: la propuesta **T-8.2b (co-piloto sobre una especialidad)** ya está cubierta desde T-4.3/T-4.4 —`cv improve` y `cv summarize` aceptan `-s`, `-f` y los límites, y los trabajos de la API (`specialty`, `offer`) también—; en la GUI llega con T-7.5b. Se retira como tarea.
    -   **PROPUESTA v1 en `docs/copilot-settings.md` (2026-08-30), APROBADA en su totalidad por el Director el 2026-08-30 (10/10)**; **S1 hecho 2026-08-30** (`[llm]` en `cv.toml` con precedencia y origen, sustitución quirúrgica, `cv llm status` con orígenes, `cv llm key set|remove|list`, informe del *spike* en `docs/copilot-providers.md`: solo Groq cumple los cinco criterios; decisión 3 pendiente del Director). **S2 hecho 2026-08-30** (registro con `openai`, `anthropic` y `groq` con evidencia C7 y cuotas publicadas; cuota viva desde cabeceras y `quota-exceeded` sin reintentos; `GET/PUT /config/llm` y `/check`; escenario `config` del arnés). **S3 hecho 2026-08-30** (pantalla «Ajustes», selector de proveedores del Co-piloto, E2E sobre `cv.toml`, guía «Configurar el co-piloto», CHANGELOG `[1.5.0]`, versión 1.5.0). Diseño: tabla `[llm]` en `cv.toml` solo para lo local (precedencia flag > entorno > fichero > defecto); registro de proveedores con evidencia C7 y cuotas publicadas; *spike* de candidatos gratuitos (Groq, GitHub Models, Cerebras) con tabla de evidencia; cuotas vivas desde cabeceras, sin telemetría; `cv llm key set|remove|list`; `GET/PUT /config/llm` y `/check`; pantalla «Ajustes»; 1.5.0 en tres sprints.
-   [x] **T-8.3: [TEMAS] Galería** ( propuesta `docs/theme-gallery.md` aprobada el 2026-08-30; S1 «temas y galería» entregado y aprobado el 2026-08-30 —`modern`, `academic`, `minimal`, metadatos de autoría, galería del portal—; S2 «instalación» entregado y aprobado el 2026-08-30 —`cv theme install` con consentimiento, lector zip/tar.gz en proceso, `.origin.json`, `cv theme verify`—; S3 «API, GUI y entrega» entregado y aprobado el 2026-08-30 —`POST /themes/install` en dos pasos, `POST /themes/{name}/verify`, «Instalar tema…» en la GUI—; **v1.6.0 publicada el 2026-08-30** con un defecto en el ejecutable (`cv theme install`), corregido en la **v1.6.1, publicada y verificada el 2026-08-30** (run 33296547995; arnés 140/140 contra el binario descargado); tarea **cerrada**): dos o tres temas adicionales (`modern`, `academic`…) y `cv theme install <url>` para temas de la comunidad. *Nota*: un tema es código Typst; la instalación desde URL pedirá consentimiento, fijará el origen y comprobará huellas (C3/C11), y el tema instalado nunca sale de `themes/` del proyecto.
    -   **PROPUESTA v1 en `docs/theme-gallery.md` (2026-08-30), pendiente de aprobación**: tres temas nuevos (`modern`, `academic`, `minimal`) con galería de imágenes en el portal; metadatos `author`/`license`/`homepage`; `cv theme install <url|ruta>` con consentimiento, descarga contenida, lector de zip/tar.gz en proceso con política de entradas cerrada, huellas fijadas en `.origin.json` (`--sha256`, `cv theme verify`); `POST /themes/install` en dos pasos; GUI mínima opcional; 1.6.0 en tres sprints; la contención de Typst no cambia.
-   [x] **T-8.4: [I+D] Spike de importación desde PDF** (**cerrada el 2026-08-30 con veredicto «Go limitado» del Director de Ingeniería y Producto**: P3 núcleo determinista; P2 descartado para el flujo por defecto; T-8.4b «Importar desde PDF» se propondrá después de T-8.5 con un corpus de PDF reales medido con el mismo arnés): un sprint de investigación con evidencia y un informe de decisión «Go / No-Go» (`docs/`), sin función en el producto hasta la decisión.
    -   PROPUESTA v1 redactada el 2026-08-30 (`docs/pdf-import-spike.md`: corpus sintético con verdad conocida, candidatos P1 heurístico / P2 co-piloto local / P3 maquetación, arnés de métricas y umbrales Go / Go limitado / No-Go fijados de antemano); **aprobada el 2026-08-30** (ocho decisiones confirmadas, umbrales fijados); **S1 entregado el 2026-08-30** (`scripts/spike/pdf-import/`, `tests/spike/pdf-import/`, `npm run spike:pdf-import`; resultados de P0/P1 en el Anexo A de la propuesta); **S1 aprobado y S2 entregado el 2026-08-30** (commits `b562bd0`, `5dfc583`, `eeea05a` y cierre: P3 y P2 medidos, grupo B completo, Anexo B); **veredicto propuesto: Go limitado** —P3 como núcleo determinista (grupo A 100 % con 0 logros inventados; tablas 100 %, barra lateral 91 %; formatos «funcional» y «prosa densa» 41/31 % por límites estructurales); P2 fuera del flujo por defecto (4–20 min por CV, falsos positivos de atribución que la verificación por código no detecta, no determinista)—, **veredicto del Product Owner el 2026-08-30: Go limitado** (aprobado «en su totalidad»); T-8.4b «Importar desde PDF» se redactará como propuesta después de T-8.5, con corpus de PDF reales de terceros antes de implementar (requisito innegociable del PO).
-   [x] **T-8.5: [OFERTAS] Ingesta desde URL**: capa de extracción de contenido (HTML, JS, PDF) y su integración en `cv analyze-offer` y `cv generate-cv --from-job-offer`. *Nota*: es la primera salida a la red del producto fuera del co-piloto: consentimiento explícito por petición (C3/C11), sin navegador embebido salvo evidencia de que hace falta (spike previo, como T-8.4), y sin cookies ni identidad del usuario. Repositorio en GitHub: `nunzi00/chameleon-cv` (fijado por el Director el 2026-08-29; `package.json` es la única fuente de verdad del nombre). **S0 recibido el 2026-08-30**: el Director facilitó siete URL (LinkedIn ×2, Jobgether ×3, Manfred ×2); seis descargadas (una responde 503) y todas con JSON-LD `JobPosting` → extracción por JSON-LD + texto visible + `og:*` (docs/offers-from-url.md §S0). **S1 (núcleo) IMPLEMENTADO el 2026-08-30**: corpus con umbrales, extractor con procedencia, descarga con guardia SSRF y CLI (`--allow-remote`+confirmación, `--save-offer`, listado); verificación en vivo hecha con tres URL reales (LinkedIn/Jobgether/Manfred, §11 de la nota); **S2 IMPLEMENTADO el 31-ago** (rutas `GET /offers` · `POST /offers/fetch` con consentimiento 403/409-estimateId · `POST /offers`; selector de offers/ y pestaña URL con diálogo y guardado en Generar; corte de cuota también en etiquetas; el plan del Co-piloto muestra modelo y razonamiento); S3 parte 1 HECHA el 31-ago (guía de ofertas con URL, tutorial «Tres ofertas» §6 ejecutable, guía api.md con offers*/import-cv, pista reforzada en Ajustes, pickers de Generar agrupados en «Afinar el contenido»); **release v1.9.0 publicada y verificada externamente el 31-ago** (tag `v1.9.0` sobre `974df52`; huellas en la cabecera del hito; cosign y atestación SLSA en verde). **CERRADA.**
-   [x] **T-8.7: [TEMAS] Más temas de CV** (orden del Director del 2026-08-30: «analiza páginas de templates de CV e implementa unos pocos más»): revisión de escritorio de las plantillas más usadas y guías ATS (registrada en `docs/theme-gallery.md` §11); cuatro temas propuestos sin solapes con los cinco existentes. **Hechos el 2026-08-30 los cuatro: `awesome`, `executive`, `tech` y `timeline`** (compilan en ambos idiomas con Typst 0.15.1, fichas e imágenes en la galería, inventario de nueve temas en pruebas y goldens). Decisiones del PO (2026-08-30): conforme con los cuatro; `tech` sin empaquetar fuentes nuevas; publicación decidida por el PO como **v1.7.0** junto con la selección explícita, el historial de ofertas y los modelos de Groq (T-8.5 pasa a la 1.8.0). **CERRADA**: la casilla se quedó sin marcar por descuido —el trabajo salió en la v1.7.0 del 30-ago— y se corrige el 1-sep; los nueve temas de entonces son hoy 37 (T-8.12 y T-8.16).
-   [x] **T-8.8: [LLM/GUI] Arrancar y parar Ollama local desde cv** (CERRADA por el PO el 2026-08-30 con las dos desviaciones aprobadas; commit 837dcf0): encargo del Director del 2026-08-30 («añade una opción para arrancar ollama local con el modelo seleccionado, y también otra para pararlo»). PROPUESTA v1 `docs/ollama-runtime.md` **APROBADA por el PO el 2026-08-30 (D1–D8 tal cual)**: `cv llm up [--model] [--runner native|docker] [--no-pull]` / `cv llm down`, `GET|POST /api/v1/llm/runtime`, botones en Ajustes → Proveedor local; runners `native` (`ollama serve` hijo, pid/log en `~/.cache/chameleon-cv/ollama/`) y `docker` (contenedor `chameleon-ollama`, imagen fijada por digest, `docker stop` conservando volumen); descarga del modelo como trabajo `pull-model` con consentimiento en la GUI; solo se para lo que arrancó cv; deshabilitado dentro del contenedor de Compose; sin shell, modelo validado. Versión 1.8.0. **IMPLEMENTADA el 2026-08-30** (núcleo `src/llm/runtime.ts` + adaptador `runtime-node.ts`, CLI, rutas, GUI, arnés `llm-runtime` con doble de `ollama`; desviaciones en `docs/ollama-runtime.md`: runner/imagen por `--runner`/entorno hasta que S3 conserve `[llm.runtime]`, códigos de salida 1/2). Runner docker verificado contra el demonio real (parar, reanudar, parar; modelo presente en `chameleon-ollama`); el runner native queda verificado por el arnés (no hay binario `ollama` en la máquina).
-   [x] **T-8.6: [GUI/WEB] Interfaz clara y moderna para la app y el portal** (CERRADA por el PO el 2026-08-30 con los cuatro sprints S1–S4 entregados; S4 = commits 6672279 + daa1232, portal desplegado y verificado en https://nunzi00.github.io/chameleon-cv/): encargo del Director del 2026-08-30 («una interfaz clara y moderna para la web y app»). Decisión del Director: diseño **desde cero en Claude Design** a partir de un brief, sin sincronizar los componentes Svelte (Claude Design ejecuta React). Brief v1 en `docs/gui-design-brief.md` (principios, arquitectura de información con barra lateral y cabecera de contexto, siete pantallas con sus estados, sistema visual, restricciones —CSS propio, sin recursos externos, ≤ 30 KB gzip—, entregables y prompts); la implementación en `gui/` y `website/` se planifica por sprints cuando lleguen los diseños. **S4 (portal) IMPLEMENTADO el 2026-08-30**: tema sin fuentes descargadas con los tokens del sistema visual, portada por ranuras (chip, comandos, captura real, «Qué hace / Qué no hace», «Tres caminos», «Temas» con miniaturas de la galería), pie y versión en la navegación, interiores pulidos; prueba de contraste AA de tokens y E2E del sitio construido (`npm run docs:e2e`, también en Pages); cerrado por el PO el 2026-08-30.
    -   Requisitos del Director (2026-08-30, durante las pruebas con su espacio de trabajo real): la oferta debe aceptar también una **URL**, y en «Analizar oferta → fichero del espacio de trabajo» debe salir un **selector con las ofertas disponibles** (`offers/`) en lugar del campo de texto libre. PROPUESTA v1 redactada el 2026-08-30 (`docs/offers-from-url.md`: extractor propio con corpus y umbrales, descarga solo `https` con consentimiento en dos pasos, `--save-offer` con cabecera de origen, `GET/POST /api/v1/offers` y `POST /api/v1/offers/fetch`, cuatro modos de oferta en la GUI; ocho decisiones en §10); **aprobada por el Director de Ingeniería y Producto el 2026-08-30** (las ocho decisiones tal como se recomendaban; prioridad sobre T-8.4b; versión 1.7.0); S0 arranca cuando Lucas aporte las seis páginas reales del corpus. **Requisito adicional del Director (2026-08-30, tarde)**: al añadir una oferta, indicar si ya se procesó, en qué fecha y con qué CV (historial de ofertas procesadas por huella del texto): **implementado el 2026-08-30** (`src/app/history.ts`, `output/historial-ofertas.json`, aviso en CLI, `history` en la API con `POST /offers/history`, aviso en Generar).
    -   **Entrega de diseño (2026-08-30):** exportación de Claude Design («Alcance y fidelidad del proyecto»): `tokens.css`, `componentes.css`, nueve pantallas y plan S1–S4, recogidos en `docs/gui-design/` (los tokens y las clases van integrados en `gui/src/app.css`; el prototipo HTML queda fuera del repositorio). **S1 CERRADO (2026-08-30):** sistema visual (tokens claro/oscuro, contraste AA comprobado por `gui/src/lib/contrast.test.ts`), `Nav.svelte` (tres grupos + Portal, plegado persistente en `cv.nav.collapsed`, forzado < 1024 px), `ContextHeader.svelte` (espacio de trabajo, cuatro chips de una sola consulta, tema en `cv.theme` aplicado en `main.ts` antes del primer render, «Apagar» con confirmación), `Dialog` con foco atrapado y `Esc`, armazón `232px 1fr` en `App.svelte`; 127 pruebas de la GUI (100 % de `gui/src/lib`), E2E 12/12 (chips en toda pantalla, tema, teclado, plegado, nada recortado a 1024 px), paquete inicial 29,9 KB gzip (presupuesto 30 KB). **S2 CERRADO (2026-08-30):** Estado (commit 25416cb), Salidas (25416cb), Fuentes (9f399d9) y Generar reescritos sobre las clases nuevas con sus estados (éxito, vacío, error, cargando); vistas puras nuevas con 100 %: `status.ts` (Typst, co-piloto, filas de temas), `sources/tree.ts` (filtro, recuentos, incidencias, huella corta, fin de línea), `generate/form.ts` (`specialtyPreview`), `generate/report.ts` (porcentaje y recuentos); 145 pruebas GUI, E2E 12/12, paquete inicial 25,7 KB gzip; capturas de la guía regeneradas. Fuera de S2 por depender de la API: «Modificado» en Salidas y «Eliminar…» (no hay fecha ni borrado en `/outputs`), «Reinstalar…» de Typst (no hay ruta), pestaña URL de la oferta (T-8.5), «Ver diferencias»/copia `.bak` en el conflicto. **S3 CERRADO (2026-08-30):** Revisiones con el antes/después completo de cada fuente (petición del Director; commit 5feb3c5), `[llm.runtime]` en Ajustes, Co-piloto (opciones en tarjeta, panel «Qué sale y a dónde», trabajos con progreso), Ajustes (fichas de proveedores con cuota en barra, lista blanca) y puerta de sesión con caducidad explicada; 157 pruebas GUI, E2E 12/12, capturas regeneradas y revisadas, paquete inicial 26,1 KB gzip. Pendiente: S4 (portal).
-   [x] **T-8.9: [CORE/GUI] Generar con la adecuación de la oferta** (CERRADA por el PO el 2026-08-30; commit d8773a4): encargo del Director del 2026-08-30 («cuando analizo una oferta, espero que al generar cv se utilicen los requisitos demostrados además de que se utilice el perfil acorde a la oferta»). PROPUESTA `docs/offer-driven-generation.md` **APROBADA por el PO el 2026-08-30 (D1–D3)**: especialidad sugerida en el análisis (CLI la imprime; la GUI la rellena si el paso 1 está vacío), evidencias demostradas conservadas frente a los límites de cantidad (`keepEvidence` por defecto, `--no-keep-evidence`), «Generar con esta adecuación» en el panel de adecuación; versión 1.8.0. **IMPLEMENTADA el 2026-08-30** (núcleo `suggestSpecialty`/`evidenceIds` + `SectionLimits.keep`; CLI `--no-keep-evidence` y línea de sugerencia; API `keepEvidence`, `report.kept`, `suggestedSpecialty`; GUI); 958 pruebas al 100 %, GUI 156, E2E 12/12, arnés 155/155 (goldens `core` y `serve` regenerados); guías de ofertas y generación.
-   [x] **T-8.10: [CORE/CLI/API/GUI] Histórico de versiones de las fuentes** (CERRADA por el PO el 2026-08-30; commit 2b2af30): encargo del Director del 2026-08-30 («cuando se actualiza un improve de skill, trabajos, proyectos… quiero almacenar en un histórico la versión que se ha modificado al completo»). PROPUESTA `docs/source-history.md` **APROBADA por el PO el 2026-08-30 (D1–D4)**: `output/historial-fuentes/<fecha>-<revisión>/<ruta>` con el fichero completo, `cambio.json` e `index.json`; desaparecen las copias `.bak` de `data/sources`; `cv history`/`show`/`restore`, `GET /history`, GUI (Revisiones enlaza; Fuentes muestra versiones anteriores con diff); restaurar crea entrada. Versión 1.8.0. **IMPLEMENTADA el 2026-08-30** (núcleo `src/app/source-history.ts`, `applyReview` sin `.bak`, `cv history`, API `/history*`, Fuentes «Historial de esta fuente», Revisiones enlaza); arnés `apply` con `history`/`show latest`/`restore latest`. Pendiente: cierre del PO.
-   [x] **T-8.11: [LLM] Evaluar Qwen3 como modelo local por defecto**: pregunta del Director del 2026-08-30 («¿por qué qwen2.5 sobre qwen3.8?»). PROPUESTA `docs/qwen3-evaluation.md` **APROBADA por el PO el 2026-08-30 (D1–D3)**: soporte explícito (`think: false` para `qwen3*`, tolerar `<think>` residual), evaluación de `qwen3:8b` (y 14b si cabe) con el arnés de IA frente a `qwen2.5:7b-instruct`; el defecto solo cambia con 16/16 y tiempo ≤ 1,5×. En curso en segundo plano. **Evaluación hecha el 2026-08-30 (§4)**: qwen2.5 16/16 en 224 s (improve 0/6 propuestas aceptadas por C2); qwen3:8b 16/16 en 289 s = 1,29× (improve 5/6 aceptadas) → cumple D2; **cambio de defecto a `qwen3:8b` CONFIRMADO por el PO para 1.8.0**, se ejecuta dentro de T-8.13 (catálogo + espejo de Hugging Face); deepseek-r1:8b (GGUF de Hugging Face): 10/16 en 783 s = 3,5×, con JSON inválido en las seis peticiones de improve y suggest tags → se queda en el catálogo sin tareas recomendadas. **Evaluación COMPLETA**; cierre pendiente del PO. **CERRADA por el PO el 2026-08-30.**
-   [x] **T-8.12: [TEMAS] Catálogo de temas: seis organizaciones y quince o más estilos** (CERRADA por el PO el 2026-08-30 con 27 temas —9 organizaciones + 18 estilos—; commits f15cc4d, f66c28b, d6f4174): encargo del Director del 2026-08-30 («quiero al menos 15 temas de PDF, y 6 tipos de organización de CV como temas»). PROPUESTA `docs/theme-catalog.md` **APROBADA por el PO el 2026-08-30 (D1–D4)**: organizaciones `chronological`, `functional`, `hybrid`, `skills-first`, `project-portfolio` y `one-page`; estilos `elegant`, `bold`, `compact-grid`, `monochrome`, `warm` y `europass-like` (21 temas); metadato `kind` en `theme.toml`; entrega en dos partes (organizaciones primero); versión 1.8.0. Pendiente de implementar tras T-8.10. **Parte 1 (organizaciones) CERRADA por el PO el 2026-08-30 (commits f15cc4d + 6079f74; el Director revisó los temas: «me gusta»)**: seis temas `organization` con Typst real en es/en, `kind` en el esquema/inventario/CLI/galería/GUI, `skillGroups[].items` y etiquetas de nivel en la vista, goldens `core`/`theme`/`typst` (+6 pasos) regenerados; pendiente la parte 2 (seis estilos) y la miniatura en el plegable de temas. **Parte 3 APROBADA por el PO el 2026-08-30 (D1–D3)** tras la petición del Director («¿podemos añadir más estilos visuales y organizaciones?»): tres organizaciones más (`education-first`, `achievements-first`, `unified-timeline` —esta exige fechas ISO en la vista, cambio aditivo—) y tres estilos más (`swiss`, `newspaper`, `pastel`) → 27 temas; orden: T-8.13 → parte 2 → parte 3 → 1.8.0. **Parte 2 (seis estilos) CERRADA por el PO el 2026-08-30 (commit f66c28b)**: `elegant`, `bold`, `compact-grid`, `monochrome`, `warm`, `europass-like` compilados con Typst real en es/en sobre el banco; galería de 21 temas; goldens `core`/`theme`/`typst`/`serve` regenerados (+6 generaciones); **Parte 3 IMPLEMENTADA el 2026-08-30**: fechas ISO en la vista (`start`/`end`/`dateIso`, aditivo), `education-first`, `achievements-first`, `unified-timeline`, `swiss`, `newspaper`, `pastel` → 27 temas (9 organizaciones + 18 estilos); galería, `cv theme list`, goldens (+6 generaciones) y cobertura 100 %; cerrada por el PO; entra en la release 1.8.0.
-   [x] **T-8.13: [LLM] Modelos locales de pensamiento en Ollama** (CERRADA por el PO el 2026-08-30; commits 8819bcf, 12a16c4, bde4d54): encargo del Director del 2026-08-30 («me gustaría que ollama tuviera varios modelos de pensamiento para este proyecto disponibles, por ejemplo deepseek»). PROPUESTA `docs/local-models.md` (catálogo de modelos locales con pensamiento —qwen3, deepseek-r1, gpt-oss—, espejo de Hugging Face cuando el registro de Ollama falla, `think` conmutable, `cv llm models`, selector en Ajustes y `GET /llm/models`); **APROBADA por el PO el 2026-08-30 (D1–D5, v1.8.0)**; se implementa tras la evaluación T-8.11 y antes de la parte 2 de T-8.12. Hallazgo: el registro de Ollama falla en esta red con blobs de ~5 GB («max retries exceeded», 5 intentos); `hf.co/unsloth/*-GGUF:Q4_K_M` descarga a la primera (Qwen3-8B y DeepSeek-R1-0528-Qwen3-8B ya en el contenedor `chameleon-ollama`). **IMPLEMENTADA el 2026-08-30** (catálogo `LOCAL_MODELS` con `qwen3:8b` por defecto, `thinkingOf` por catálogo y familia, espejo de Hugging Face con alias en `cv llm up` y `--source`, `[llm] think`/`CHAMELEON_LLM_THINK`, `cv llm models [--json]`, `GET /llm/models`, selector y casilla en Ajustes; arnés `llm-runtime` con nueve pasos nuevos y el doble de Ollama con `cp` y registro caído; goldens regenerados por el cambio de defecto); pendiente del cierre del PO. 
-   [x] **T-8.14: [LLM/GUI] Runtime de Ollama adaptativo** (CERRADA por el PO el 2026-08-30; commit 3cb077f): encargo del Director del 2026-08-30 («en copilot ajustes podemos arrancar/parar ollama, quiero que detecte si dispone de binario y si no pruebe con docker para que se adapte a todos los entornos»). PROPUESTA `docs/ollama-runtime-adaptive.md` (candidatos con motivo en estado/CLI/API, preferencias con respaldo, respaldo automático al fallar el arranque, Ajustes con la vía y el motivo; 1.8.1); aprobada D1–D5 y entregada: candidatos+plan en estado/CLI/API/Ajustes, preferencias con respaldo, `--runner` estricto, respaldo al fallar el arranque en ambos sentidos; release 1.8.1.
-   [x] **T-8.15: [LLM] Proveedor remoto Gemini, pendiente de verificación al alta** (encargo del Director del 31-ago: «añade la posibilidad de proveedor externo gemini»): registrado en el catálogo como `pending-verification` sobre la API compatible OpenAI de Google (rutas propias sin `/v1`), con aviso permanente de uso de datos del plan gratuito (`dataNote` en estado, CLI, Ajustes y el 409 de consentimiento), modelos gemini-2.5-flash/pro sin recomendaciones (D3), suelo de tokens de salida por modelo y registro inyectable en pruebas; diseño en `docs/gemini-provider.md` (§3.5 IMPLEMENTADO); se activará como Groq (§9 del acta `docs/copilot-providers.md`: alta sin tarjeta confirmada por el Director el 31-ago) cuando un humano verifique el alta de Gemini.
-   [x] **T-8.18: [IMPORT/GUI] Botón «Refinar con el co-piloto» en la pantalla web de importación** (prioridad del PO tras la 1.10.0 y orden directa del Director el 31-ago: «adelante con el botón refinar con el co-piloto»): nota de diseño **APROBADA D1–D3 por el PO el 31-ago** (D1 trabajo `import-map` aparte, no una cabecera en `POST /import-cv`; D2 el trabajo relee el `README.md` del borrador; D3 las propuestas van a ese mismo informe); **IMPLEMENTADA el 31-ago**: `POST /api/v1/jobs/import-map` con consentimiento, progreso y cancelación, lectura y actualización del informe en `src/import/draft.ts` (`unplacedFromReport`, `withProposals`), núcleo `src/app/import-map.ts`, botón con selector de proveedor y diálogo de consentimiento en la pantalla, pasos nuevos en el arnés de la API y guías del portal; verificado en vivo contra qwen3:8b desde el navegador. De paso: el informe de la pantalla usaba una clase de estilo inexistente y estiraba la tarjeta.
-   [x] **T-8.16: [TEMAS] Diez temas más para los PDF** (**CERRADA por el PO el 2026-08-31**; commit `59bad9a`) (encargo del Director del 31-ago: «quiero 10 temas más para los pdfs»): propuesta al PO con el reparto organizaciones/estilos sobre el catálogo de 27 (mecánica de T-8.12: fichas, galería, prueba común con Typst real, goldens); **PROPUESTA `docs/theme-catalog-2.md` APROBADA por el PO el 31-ago (D1: 4 organizaciones + 6 estilos → 37 temas; D2: misma mecánica; D3: release 1.10.0)**; **IMPLEMENTADA el 31-ago**: 27 → 37 temas (13 organizaciones + 24 estilos) generados con la mecánica de T-8.12 desde el bloque común de `chronological`; los diez compilan con Typst real en es/en con texto extraíble (lección aplicada: `gazette` perdió el `tracking` de sus títulos porque partía «EX PERIENCIA» en la extracción; `mono-grid` usa el peso regular en el nombre porque DejaVu Sans Mono no tiene semibold y caía a una proporcional); galería regenerada con 37 fichas e imágenes, inventarios y goldens (core/typst/theme/serve) al día.
-   [x] **T-8.17: [LLM/GUI] Conmutador de remotos en Ajustes del co-piloto** (**CERRADA por el PO el 2026-08-31**; commit `59bad9a`) (encargo del Director del 31-ago: «habilitar/deshabilitar remotos desde ajustes»): hoy `--allow-remote` es una bandera de arranque del servidor (frontera C3); la propuesta debe decidir si se persiste (¿`[llm] allow_remote` en cv.toml leído al arrancar?) o se conmuta en caliente con aviso explícito, y qué muestra Ajustes; **PROPUESTA `docs/remote-toggle.md` APROBADA por el PO el 31-ago: opción A** (persistir `[serve] allow_remote` en cv.toml leído SOLO al arrancar; `--allow-remote` de la CLI siempre gana; Ajustes lo muestra y avisa del reinicio; la conmutación en caliente queda descartada por C3); **IMPLEMENTADA el 31-ago**: tabla `[serve]` en `cv.toml` (esquema, serialización y sustitución quirúrgica compartida con `[llm]`), lectura al arrancar en `cv serve` con `--allow-remote`/`--no-allow-remote` ganando siempre (bandera de tres estados por `getOptionValueSource`), `PUT /api/v1/config/serve` con `If-Match`, `GET /config/llm` devuelve `remote: { allowed, configured, pending }`, botón y aviso de reinicio en Ajustes, pasos nuevos en el arnés de la API y guías del portal (copilot-settings, api).
-   [x] **T-8.4b: [IMPORT] Importar un CV existente (PDF/DOCX) a las fuentes** (**CERRADA por el PO el 2026-08-31**: F1 en v1.9.0 y F2 en `6356145`; desviación del formato de revisión ACEPTADA; botón web «Refinar con el co-piloto» planificado para 1.10.0): ORDENADA por el Director el 2026-08-30 («necesito que proceses […] la parte de importación de cv; creo que la ayuda de llm puede ser de ayuda»). PROPUESTA `docs/cv-import.md` (núcleo determinista P3 del spike T-8.4 + asistente opcional del co-piloto con propuestas como revisión C2; borrador en `import/`, nunca sobre `data/sources/`; DOCX mínimo; los PDF de los 27 temas como corpus); **APROBADA por el PO el 2026-08-30 (D1–D5)**; por orden del Director del mismo día se implementa tras S1 (antes que la GUI de ofertas). **Núcleo IMPLEMENTADO el 2026-08-30**: `src/import/` (maquetación P3 del spike endurecida: items con worker acotado, orden de lectura, estructurador con procedencia), `cv import-cv <pdf|docx>` con borrador en `import/<nombre>/` validable con `cv build --data` (README con lo degradado y lo sin situar, banner de borrador, --name/--replace), DOCX mínimo por `word/document.xml` con el lector de zip contenido; escenario `import-cv` en el arnés; **pantalla web «Importar CV»** (grupo Perfil) sobre `POST /import-cv` binario con el mismo núcleo (misma noche, a petición del Director); **F1 completa y publicada en v1.9.0** (los banners de borrador se retiraron después: la procedencia vive solo en el README del borrador). **F2 ENTREGADA el 31-ago** (medida sobre 4 CV rellenos descargados a `build/import-corpus/`, nunca versionado): `--copilot` propone sección para lo sin situar con vocabulario cerrado y verificación por código (C2), sin aplicar nada y con consentimiento de coste si el proveedor es remoto —verificado en vivo con qwen3:8b: propuso «descartar» para la cabecera y «experiencia» para las dos entradas de CAMPUS INVOLVEMENT—; heurística: matices en los títulos de sección y su variante espaciada, cabecera espaciada desconocida que cierra sección, fecha de graduación suelta con aviso, paréntesis en `splitNames`; nuevo `src/import/quality.ts` con avisos de calidad (OCR sucio, plantilla sin rellenar, texto demasiado corto, sin entradas); guía «Importar un CV que ya tienes» en el portal. Precisión en el CV de referencia: 0 exp · 4 form falsas → **2 exp · 1 form reales**.

Observaciones técnicas del Director Técnico (2026-08-29), a resolver en la especificación de cada tarea antes de implementarla:

1.  **Numeración de cánones.** El plan cita «Canon C12: The Core is the Product»; C12 es «Validar el proceso, no el resultado» (canonizado el 2026-08-29). **Canonizado como C14 por el Director el 2026-08-29** («El núcleo es el producto»: el núcleo lógico permanece agnóstico de cualquier interfaz, es su propia API y nunca depende de un cliente concreto). La referencia a T-5.3 como decisión previa sobre la GUI no coincide con este ROADMAP (T-5.3 fue `cv theme`).
2.  **Loopback en Docker (canon C3).** `cv` rechaza cualquier `CHAMELEON_LLM_BASE_URL` que no sea local; en Compose, `http://ollama:11434` no lo es. Propuesta: `network_mode: "service:ollama"` (espacio de red compartido, `127.0.0.1:11434` sigue siendo loopback) antes que relajar la guarda.
3.  **Modelo recomendado.** El producto está validado con `qwen2.5:7b-instruct` (valor por defecto; arnés de IA 16/16). Recomendar `llama3:8b` exige antes pasar el arnés de IA con él (C12/C13).
4.  **Imagen final.** El ejecutable SEA ya embebe Node: la etapa final no necesita una imagen de Node (basta `debian:*-slim` o `distroless/cc`, glibc ≥ 2.28); Typst instalado en la imagen (`cv typst install` durante el build); mapeo de UID para que los ficheros 0600 del volumen pertenezcan al usuario anfitrión; imágenes base fijadas por digest; atestación también de la imagen.
5.  **`cv serve` cambia el modelo de amenazas** (de «sin red» a servidor HTTP local): solo `127.0.0.1`, token de sesión aleatorio, comprobación de `Origin` y sin CORS abierto (CSRF y *DNS rebinding* contra una API que escribe ficheros y puede consumir proveedores remotos); identificadores relativos al espacio de trabajo en lugar de rutas (`POST /api/improve` con «la ruta a un fichero» es un *path traversal*); consentimiento C11 también en la GUI; y una clarificación de C9: editar desde la GUI es el usuario escribiendo sus fuentes por acción explícita, nunca la IA.
6.  **Alcance de dependencias.** Cada pilar trae su toolchain (VitePress, Docker, servidor HTTP, SPA): se mantendrán aislados del binario (el producto no crece) y con la misma disciplina de cadena de suministro (versiones fijadas, Dependabot, acciones por SHA, imágenes por digest).
