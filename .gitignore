# Dependencias
node_modules/

# Artefactos de compilación (solo en la raíz: los artefactos esperados de tests/acceptance sí se versionan)
/dist/
/build/
*.tsbuildinfo

# CVs generados: nunca se versionan (solo en la raíz)
/output/

# Tests y cobertura
coverage/

# La interfaz web construida (viaja en el ejecutable; se genera con npm run gui:build)
/gui/dist/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Entorno local (secretos)
.env
.env.*
!.env.example

# Sistema operativo / editores
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Artefacto canónico generado por `build-profile` (datos personales en claro; solo en la raíz)
/data/dist/

# Espacio de trabajo de Docker Compose (tus datos personales; docs/docker.md)
/my-profile/
